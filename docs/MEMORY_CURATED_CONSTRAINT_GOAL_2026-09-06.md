/goal Make one explicitly selected Scry project constraint survive durable ingestion and appear, with exact source attribution, in a fresh session's orientation. An edited constraint must be presented correctly and must not leak into an unrelated project's orientation. Deliver a tested local change, not a deployment or a replacement memory engine.

Authoritative sources:
- The September 6 reset supersedes the old goal: retain safety work, freeze the admission prototype, pursue one workflow outcome.
- Read repository instructions, docs/DECISIONS.md, docs/MEMORY_WORKFLOW_ASSESSMENT_2026-09-04.md (curated-memory gap), and docs/memory-repairs/normal-admission-integration-progress-2026-09-06.md. The assessment is dated evidence, not an authorized backlog. The 41-group consolidation is excluded.

Done when:
1. Before implementation, preserve the exact private candidate at /tmp/scry-complete-admission-sep06.6qYb7K/code and the corrected freeze at /tmp/scry-owned-corrected-sep06.7Aah7P in a durable, local, non-overwriting archive outside /tmp. Include hashes and test/review evidence; verify against originals. Inventory known binaries/backups without modifying them. Label historical/missing evidence; do not reconstruct it.
2. A concise reset inventory separates delivered code, private experiments, confirmed regressions, unclassified failures and deferred work. Retain the 71-failure record and value-evidence counterexample without making their resolution part of this goal.
3. One explicit, opt-in file mapped to this repository enters the existing durable ingestion path. No home-directory crawl or guessed project ownership. Source path, revision and repository provenance survive persistence and are exposed in orientation.
4. An end-to-end test exercises the real ingestion, queue, storage and orientation paths in isolation: ingest a curated rule, finish processing, restart/reopen, then invoke orientation from a new client without conversation history. The rule and attributable source appear within the normal output budget.
5. Edit the same source in a fixture and repeat: orientation presents the updated rule, not the obsolete rule as current. Prior evidence remains recoverable. Re-ingesting unchanged content creates no duplicate effects; ingestion failure cannot advance the cursor past unaccepted work.
6. An unrelated repository's orientation excludes the rule. Existing history preservation, alias ownership, repository attestation and queue behavior do not regress.
7. Record source-to-orientation latency, commands, revision and outputs for original/edit/isolation cases. Required checks pass. Document limitations and a reversible, opt-in usage procedure. Stop for review; do not continue into another milestone.

Boundaries:
- Work from verified repository source, not the private adapter. Preserve unrelated edits and the untracked workflow assessment.
- No live-store writes/repairs/restores, deployment, daemon restarts, hook/config changes, hosted-provider calls, installs, pushes or deletion. Use isolated stores, sockets and fixture roots; deterministic extraction substitutes must be disclosed as test evidence, not live model-quality proof.
- No admission redesign, schema migration, collision consolidation, broad ingestion expansion, ranking benchmark tuning or new embedding system. Do not weaken tests to obtain green. If this slice requires changing identity/temporal policy or bypassing the durable path, stop and present the decision.

Validation loop:
Baseline then checkpoint meaningful changes with focused regressions, CGO_ENABLED=0 go test ./... and CGO_ENABLED=0 go vet ./.... Retain logs and distinguish baseline/environment failures from new ones; do not absorb unrelated repairs. Maintain a compact acceptance checklist. Finish only with evidence for every criterion; otherwise report the exact missing authority or product decision.
