# Child dev-client spellings: bounded negative-ownership proposal

PROPOSED_DROP for only Child's `Expo dev client`, `dev-client`, and `dev client`;
independent semantic review PENDING. Not an apply manifest. Do not rehome to
the whole mobile project, Metro, a script, package identifier or inferred new
client identity. Do not relocate, invalidate or discard any fact. There are
five unavailable original transcripts; this is not a complete 18-original-source
closure and does not certify their asserted details.

## Snapshot and complete stored closure

Root restored complete Mini backup `memory-20260905T233353Z.badger`,
73,971,229 bytes, SHA `907b9ae1e3210ac3840271e66ab95e34c9a24838863031812937c161bd076703`,
using deployed d1f0a958 source. Read-only discovery across every serialized
current and historical fact for `(?i)expo[ _-]*dev[ _-]*client|dev[ _-]*client`
recovers 30 matching rows, 18 complete episode records, 204 distinct complete
companion payloads, and 29 selected endpoint metadata records. Root has now
read all those complete objects, not just summaries or current facts.
Private evidence: `/tmp/scry-alias-rejection-deploy-sep05.pPmnn3/dev-client-discovery-233353/`.
Physical JSON file SHA-256 values (not typed Go-fact fingerprints):

| File | SHA-256 |
|---|---|
| matching.json | 0742e21dfbd1aa4da297a49d076dfad2ada7e172dc7af68e693af5c9a1d537f0 |
| companions.json | 2a2dc6289c57347d2f32356b1b8a7d8200a17801e99d9f9f4f1c1d47981555d1 |
| episodes.json | 9fa75de33835b9f362107a8e5078f4cf6372611887df8ab8aaa3ef02bc6d9aa6 |
| endpoints.json | fe4e9eb2308b83e11302b3e99d65e3c5c34b31f647348f3c4e3f9763ad4239c6 |

Complete 30,725-entity and 52,113-claim scans find only Child listing either
normalized spelling (`expo-dev-client`, `dev-client`), both indexed to Child.
The two generic separator variants name one key. No outside listing would be
stranded by removing only these exact Child spellings and their claims. Current
Child metadata has 40 aliases, a false survtest branch description, and mixed
repository references; none of that metadata is approved by this proposal.
Any eventual operation must regenerate full Expected inputs on a fresh stable
source, preserve prior rejection/retirement decisions and pass independent
technical and actual-live gates. No live operation has occurred here.

## Available original-source evidence

Twelve complete redacted human/assistant projections reproduce exact stored
episode IDs with the unchanged production distiller. Root read every one.
The prior five are recorded in `child-dev-client-five-source-closure-2026-09-05.json`
and described in `child-dev-client-review-in-progress-2026-09-05.md`.
Seven additional originals, with full SourceRef/raw-span/projection hashes,
are in `child-dev-client-seven-source-closure-2026-09-05.json` and the private
`dev-client-seven-additional-sources/` directory. These are original messages,
not current commands or instructions. Omitted tool/reasoning payloads and
images are not claimed semantically read.

- `2adf8ae8`, `4c6a685e`, `8d9bb567`, `e7b3048e`, `85dcc743` explicitly separate
  Herd Laravel and its API from the mobile checkout, installed client/native
  modules and Metro bundle. App-client installation is not Laravel deployment.
- `11409342` separates mobile and web photo changes before implementing the
  single-device Android launcher. The dev client is an installed mobile
  prerequisite, not the shared Laravel backend or launcher script.
- `4c41ca28` starts with a Laravel-issued handshake consumed by the mobile
  Pipecat client; it traces missing backend env, phone loopback routing, and
  finally an unresolved Daily/native transport hang. Preserve the source's
  changing hypotheses and final timeout/instrumentation, not a fabricated
  end-to-end repair. Neither Laravel nor Python runtime is the native client.
- `8374ea56` distinguishes a stale installed native WebRTC binary from the
  separate import-patch error and Metro cache. It records Xcode destination
  troubleshooting and a proposed separate native rebuild process. Do not
  turn a rebuild recommendation into evidence of successful device validation.
- `b580d6b8` explicitly lists separate Laravel and mobile directories and Helm
  commands. It settles on a fixed Laravel port after two rejected choices.
  The mobile dev-client command consumes the API; it does not name that API.
- `9927145e` separates the shared local stack from iOS/Android one-time client
  installation and remote beta testing. It fixes a mobile env scheme/port
  while preserving the user's deferred latency/product choice. The stored
  `hermes-ops status ready` companion is a misattached Helm-stack statement,
  not repository identity evidence.
- `99659758` concerns the native mobile STT pipeline, async finalization,
  permissions and rebuilding. Its reported package-import repair subsequently
  fails and is partly reverted, ending with another unresolved import. Keep
  that chronology; do not endorse every intermediate claimed fix.
- `f104e373` is a distinct Program Health web/API/integrations-simulator
  discussion. Its regex match is a contaminated `ios-sim-dev-client` attribute
  on a world.sqlite rebuild fact. The source says schema-stale sim data, not
  an Expo client or Child Laravel. It also does not support the companions
  misattached to battery-designer-web/mobile. No third-project repair follows.

The full available seed `reference_android_emulator_driving.md` was read;
SHA `d7547135327e87f7bec4092f5895dbc0299aae3c54817ebcd9852af59af09f76`.
Its frontmatter modified instant 2026-07-26T01:57:29.028Z agrees within
milliseconds with stored source occurrence 2026-07-25T21:57:29.036291333-04:00;
this is not a claim to reproduce a historical seed revision. It expressly
names childscribe-mobile and the installed native package, distinguishes
Metro/ADB/emulator from the Laravel API, and describes a stale package
coexisting with the current build. It is a separate source, not a replacement
for the missing originating transcript. Preserve differing historical Android
device-argument assertions; this memory audit does not settle SDK syntax.

## Missing originals and retained uncertainty

These recorded original paths do not exist. A bounded filename search under
local `.claude`, `.codex`, `.scry` and Mini `.scry` found no JSONL or JSONL.gz
with these UUIDs. No global archive or recovery claim is made.

| Episode | Missing filename under its recorded Claude project |
|---|---|
| 2c98a663 | scribe/28fd8aac-8844-48bc-8fa4-76b9bd3159a3.jsonl |
| 36dd2fb1 | scribe/97527427-4a9b-4f8e-be53-63192d0a4acc.jsonl |
| 42f33e16 | childscribe-mobile/5309ca31-8ebb-4edd-b7c0-c3eaa013d2ac.jsonl |
| 7d974c45 | scribe/e0ae9fd1-9344-4ebe-8dcb-53aa913b044f.jsonl |
| cf75abbc | scribe/23ddc3d9-7df7-4eed-ab9e-148bc1405c89.jsonl |

Exact full paths/ranges and complete stored episode objects remain in
`episodes.json`. Their full companion facts were read. Stored narratives
describe Android installation, mobile motion, native package/AASA mismatch,
separate feedback/Forge/Fly/mobile deployments and package renaming. They do
not independently establish that Laravel is an Expo native client; nonetheless
their unavailable details must not be upgraded to verified original evidence.
All these facts remain unchanged. Any reviewer finding that the missing
originals defeat the bounded negative must block the proposal, not silently
weaken the evidentiary bar.

## Disposition rationale and limits

The negative is grounded in multiple exact originals explicitly separating
backend from native client, not a store-scale English name rule, fact count,
cwd inference or merely the absence of another listing. `Expo dev client`
is the mobile build/runtime surface in this evidence; `dev-client` and
`dev client` are generic labels for that client, not alternate names of a
Laravel service. All three are proposed for owner-specific rejection on Child
only. No positive recipient is inferred: mobile project, native build,
installed runtime, script, Metro bundler and bundle identifier remain distinct.
Future negative decisions must not globally ban the phrase or strand another
established claimant. Every misattached/historical/source-conflicting fact
and contaminated entity field is preserved for separate review.

This proposal does not approve any Child fact correction, outside alias or
metadata edit, retrospective status invalidation, source reconstruction,
transcript retention, alias backfill or broad hygiene repair. Semantic review
is pending; a successful review still is not a live apply authorization without
the exact fresh technical manifest, restored replica and actual post-state gate.
