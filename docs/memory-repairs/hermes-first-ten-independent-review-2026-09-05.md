# FAIL submitted disposition set — six endpoint keeps, one proposal, three unresolved

The evidence supports six endpoint keeps with the limits below and the Advocates-to-Forge destination proposal. Records 2, 7, and 10 must be `UNRESOLVED`, not settled `KEEP_ENDPOINTS_WITH_SEMANTIC_CAVEAT` dispositions. Their own evidence contradicts the stored relation roles/direction. This is an ownership-only review of ten records, not a completed 857-fact audit, factual endorsement of every sentence, or live mutation approval.

Pinned inputs:

- Snapshot `/tmp/scry-migration0160-fresh-sep05.teYtyC/memory-20260905T202741Z.badger`, SHA-256 `b87def29a4334517299b55e9fc288de141f509b873c4afbdfd79bd101208ae06`.
- Submitted review `docs/memory-repairs/hermes-first-ten-review-2026-09-05.json`, SHA-256 `d66f8d8f9e081b3c39789b36c5aae7d3233adb87d58d7528d51f9fee582adfc5`.
- Rubric `docs/memory-repairs/hermes-ownership-rubric-2026-09-05.md`, SHA-256 `6b5fa340896aad79629c91940d69e490fb6c091a43ec474258c45ef9c2b9939c`.
- Full local artifact `/Users/jeff/workspace/hermes/hermes-ops/docs/superpowers/specs/2026-09-02-hermes-agent-templates-design.md`, SHA-256 `e1e1200c31374c55d1628b7a4e7c13a5755858d83f28cbfc7274a442b76b8752`.

I restored the snapshot into my own new temporary store using an independently extracted `393eeec` archive. Every one of the ten old raw keys exists and its complete stored fact JSON and SHA-256 exactly match the submitted record. The restore independently contains 857 unique current/history facts touching the trio. I read the nine complete source episode objects, all endpoint metadata, all 142 same-episode companion fact objects, the two additional Advocates/Forge episode objects, and the full local spec. The scope is those ten decisions; reading companions does not approve their ownership. Scry recall was used only for preliminary unknown-referent orientation; the pinned snapshot and listed evidence determine these verdicts.

Exact old keys, full hashes, unchanged fact payloads, submitted dispositions, and independent per-record reasoning are in `per-record-verdicts.json`. The following row numbers preserve the input order.

| Record | Independent verdict | Finding |
| --- | --- | --- |
| 1. Goal artifact `requires hermes-ops` | PASS KEEP_ENDPOINTS | Both episodes identify the continuation goal; the full goal explicitly includes this repository as an audit subject. This is an audit requirement, not repository-as-runtime confusion. The sentence also names the other two subjects. |
| 2. Address-suffix rule `merged_into mac-mini` | UNRESOLVED | Destination identifies the repaired machine, but the evidence says a duplicate machine record was merged. The source is a rule/decision, not that duplicate. Neither `merged_into` nor raw `merged` supports treating the rule as the thing absorbed into the machine. |
| 3. Advocates `deployed_on hermes-ops` | PASS PROPOSAL_ONLY for `forge` | The original production-hotfix episode, companions, production-access seed, and later Forge deployment episode identify Advocates on Laravel Forge. Hermes repository is not its host. Snapshot target key is unoccupied; no live relocation is approved. |
| 4. Agent template `deployed_on mac-mini` | PASS OWNERSHIP ONLY, proposal caveat required | The proposed unit and intended Mini host are supported. The episode recommends the unit and the strategy companion says its approach is blocked; no historical completed deployment is proved. Do not count this as a deployment-validity PASS. |
| 5. Agent template `located_at hermes-ops` | PASS OWNERSHIP ONLY, proposal caveat required | The recommendation places the unit in the repository; the full later spec places manifests in `deploy/agents`. This matches the rubric's explicit initial example. Later document existence does not prove implementation at the earlier fact time. |
| 6. Agent-template spec `part_of hermes-ops` | PASS KEEP_ENDPOINTS | Source episode identifies the committed design artifact in a separate repository worktree; the complete named document corroborates repository identity. No runtime deployment or historical branch ancestry is inferred. |
| 7. `ai-sync.py lacks mac-mini` | UNRESOLVED | The source episode establishes Kimi installation without dotfiles/ai-sync MCP changes. The sentence concerns one Kimi MCP configuration output. It does not support “the tool lacks the machine,” nor absence of the whole tool. Source granularity and canonical relation need explicit review. |
| 8. Open WebUI `deployed_on mac-mini` | PASS KEEP_ENDPOINTS | The hardening episode, named launchd service and paired proxy/cluster companions distinguish the Mini service from Linux systemd workers. This is historical ownership, not a new current health test. |
| 9. Labeling proxy `deployed_on mac-mini` | PASS KEEP_ENDPOINTS | Same evidence identifies `ai.halo.proxy` on Mini port 9998, separately from the worker/coordinator. No inference from generic “halo” naming or another contaminated entity is needed. |
| 10. `alert replaced_by hermes` | UNRESOLVED | The manual episode describes Hermes deduplicating alerts. Hermes is the acting gateway; alert is the object. `replaced_by` is unsupported, and raw `deduplicates` has the wrong source/destination direction as stored. |

The rubric explicitly says: “Conflicting episode evidence, multiple plausible owners, or unsupported relation semantics receive `UNRESOLVED`, with concrete alternatives and missing evidence. This does not authorize a guessed move or silent keep.” Records 2, 7, and 10 fall under that instruction. Recognizing the referenced machine or gateway is useful partial evidence, but it does not settle the source role or directional edge. A caveat can preserve that partial finding; it cannot upgrade these records to completed keeps. This does not authorize relation edits, endpoint swaps, recreation of retired machine entities, or a new config entity.

For record 2, a later reviewed representation could describe the rule as an actor with an appropriate relation or describe the historical duplicate as the merged object. Those alternatives are materially different and are not chosen here. For record 7, the missing piece is a reviewed representation of one configuration omission, without generalizing it to the whole sync tool. For record 10, the actor/object roles are supported but a closed-vocabulary relation/direction repair remains unreviewed. In particular, merely swapping endpoints while retaining `replaced_by` does not fix it.

Record 3 target check: `fa:advocates:deployed_on:forge:1781291442000000000` is absent in this pinned restore. Exact `Forge` and `Laravel Forge` routes resolve to the existing `forge` service, whose metadata and independent episode evidence identify Laravel Forge. This supports the explicit destination proposal; it is not a fresh live conflict check or an apply manifest. All other Advocates edges remain out of scope.

The local agent-template document is later evidence of a repository design, not proof that the recommendation had already been deployed. Its deployment architecture and rollout reinforce the difference between repository contents, gateway configuration, and host resources. The current contaminated `hermes-ops` description was not accepted as a boundary definition. Exact snapshot aliases `hermes-ops`, `hermes`, and `mac-mini` resolve to three different existing entities; no merge or alias cleanup is proposed.

No store-scale keyword, sentence, type, cwd, or fact-count ownership rule was run. No shared repository or live store was changed, no remote research/provider call was made, and no messages to third parties were sent. All local writes are evidence files and my own restored replica. `TestIndependentHermesTenEvidence` passes exact key/payload/hash checks; extracted evidence is in `results/verified-records.json`, `results/episodes.json`, `results/companions.json`, `results/endpoint-metadata.json`, `results/additional-episodes.json`, and `results/summary.json`.

Disposition: revise the review ledger so records 2, 7, and 10 remain explicitly unresolved; retain the other six endpoint judgments with their stated temporal limits, and retain record 3 as a proposal only. No permission for live movement follows from this report. The remaining 847 trio records and any later drift are not graded here.
