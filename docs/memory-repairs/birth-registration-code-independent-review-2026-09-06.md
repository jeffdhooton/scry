# Private birth registration — independent bounded code disproof

2026-09-06. GO for the exact pinned private, uncalled ordered registration accounting unit under its complete frozen contract. No violation of that bounded contract was proved. This is not production admission, support/undo/lifecycle policy, alias authority, adoption, Force selection, EP provenance, deployment, cleanup, or a complete memory-quality goal verdict.

I ran and read `scry memory orient --cwd .` before other work, then read the complete active goal objective, frozen registration contract, frozen design, complete independent design review, archived serialized coordinator code review, and relevant complete registration, input, observation, generation, identity/fact ledger, writer history, journal and inventory sources. I inspected the transaction/commit/publication machinery. I independently exported baseline `e13f7ae38d3cd6500df2ec157430da29dd699eb5` using `git archive e13f7ae | tar -x -C /tmp/scry-birth-code-disproof.VmQqTj`, then copied exactly the four pinned new candidate Go files with apply_patch. I authored only one new synthetic test file and this report in that private export, using apply_patch and gofmt on my new test.

All supplied source/test and document pins matched before review and after execution. A complete comparison against baseline git blobs verified all 576 archived files unchanged, including every original test. Source search for runBirthRegistration/newBirthRegistry/birthRegistry/registeredBirth outside tests finds only the new implementation file: there is no direct or indirect production caller. The candidate and shared checkout were not edited. No real database, restored live replica, SSH, providers, spend, rooms, remember, sweeps, configuration, backup, deployment, or agents were used. Every executed database fixture is synthetic. Builder full-suite and vet results were not substituted for independent execution.

## Executed evidence

Both commands ran in the private export with `set -o pipefail`, full stderr/stdout retained with tee, and `CGO_ENABLED=0`:

```
CGO_ENABLED=0 go test ./internal/memory/store -run 'TestBirthCodeIndependent|TestBirthRegistration' -count=1 -v 2>&1 | tee birth-code-targeted.log
CGO_ENABLED=0 go test ./... -count=1 2>&1 | tee birth-code-fullsuite.log
```

The targeted command passed on its first execution, exit 0, store 2.437s. It ran six independently authored adversarial groups and all twelve supplied groups unchanged. The full no-CGO suite passed on its first execution, exit 0: store 50.827s, resolver 15.246s, daemon 28.180s. There were no independent test failures, test weakening, or implementation fixes during this review.

The builder's earlier initial targeted failure is preserved separately at `/tmp/scry-birth-registration-sep06.vZeC2U/birth-registration-initial-targeted.log`, which I read completely and hashed. All seven deferral subcases failed at missing-source public PutFact fixture setup (`memory: fact source "atlas": memory: not found`), before reaching registration. The lead reports correcting only that new fixture to seed the deliberately dangling legacy row directly; the candidate implementation pin did not change. The initial log is not a registration PASS and is not erased by the corrected run. My independent dangling-reference fixtures also deliberately use raw synthetic seeds, because the public writer correctly refuses that missing source. No original test was altered to construct these fixtures.

## Findings and attempted disproofs

1. Initialization and finalization are fixed in the wrapper. The wrapper owns revision bytes before entering the serialized owner, decodes canonical key/body, creates both complete ledgers, then invokes BODY. It exposes no finalizer parameter. Source and tests show the only finalizing callback runs the registry freeze, which verifies both ledgers, ordered history and exact creations. A report returns only after the owner's actual Badger commit and event publication complete. `TestBirthCodeIndependentCaptureAndDeferredDeletion` checks captured alias rows, fact count and initially empty writer history at callback entry. The supplied malformed-revision tests refuse before BODY is called. My untracked raw FA test proves the fixed finalizer refuses an otherwise valid registered identity when a fact is written without its owned ledger; supplied tests do the corresponding persistent en/al omission cases.

2. Birth derivation preserves original parsed occurrence identity. Register derives only declaration Name or the original endpoint selected by canonical observation Side, using the owned revision's episode and instant. Supersedes is never a birth origin. The independent original-role test uses the inverse-relation fixture and a value-source-shaped fact, registers original src/dst independently, and verifies exact original names, endpoint origin and primary roles. It proposes primary and both supersedes links from one carrying observation to two candidates; exact duplicates collapse per candidate, distinct roles remain, and no rows change. This is synthetic original-input preservation, not a production resolver flip integration test. The supplied same-name/same-fact src/dst case confirms that equal birth tuples do not make different full observations exact retries. Supplied materialization tests confirm exact-first retry returns the same pointer after en creation.

3. Ordering is based on recorded history, not final absence. The registry scans the owned writer's complete earlier entries before first registration and records the current entry position. Freeze requires prior registration for every baseline-absent en/al actor and checks entry sequence. Supplied cases cover persistent and reverted alias writes, created/deleted en, unmatched aliases/entities and transient unmatched creations. My independent interleaved-actor test proves a second registration records actual position 2 after two other-actor entries; subsequent interleaved stable writes and deletion retain correct created/final-present accounting. A pre-registration deletion of an old alias on behalf of an absent actor refuses, even though no en ever exists for that actor. A retry after registered creation and deletion remains a descriptive same handle; it does not allow recreation.

4. Exact creation coverage includes transient history. Every absent-to-present en must have a matching registered slug/name/creation instant; every later en after-image keeps that tuple. At most one recorded creation is accepted. My independent test writes the correct entity, temporarily replaces name or creation instant, then restores exact correct bytes: freeze still refuses and rolls back all rows. Deleting then recreating via the lower owned ledger likewise refuses. Supplied tests cover wrong first name/time, stable replacement, unmaterialized proposals, and a valid created-then-deleted candidate. Existing baseline entities take existing-not-new with no handle or new birth certificate, even on the supplied identical-input replay. This does not authorize their metadata or alias changes.

5. Baseline reference deferral stays normal and useful, and cannot be erased by BODY. My independent current/historical destination-reference tests delete the baseline reference through the owned fact ledger, then register two distinct observations for its missing destination and an exact repeat. Both distinct observations still defer, retain their original endpoint/declaration roles and canonical bytes, deduplicate only the exact repeat, and leave no handle or poison. An unrelated registered Borealis entity commits in the same transaction. The deletion itself commits in this synthetic accounting test and is reported by the fact ledger, proving baseline authority after deletion without relying only on a deliberately aborted transaction. This does not authorize future production fact deletion; preservation/undo policy is outside this unit and remains mandatory. The old alias row is preserved and remains baseline relationship data, not birth authority.

6. Cached result paths do not bypass canonical or scope validation. My independent matrix first obtains registered, existing-not-new, or deferred disposition, then repeats with prior poison, noncanonical raw bytes, or a separately canonical observation from changed revision metadata. Every local repeat returns the registration sentinel and an empty decision, and catching it does not permit commit. The prior error retains exact outer precedence. A valid previously deferred observation followed by opaque `rt:atlas` occupancy also refuses instead of using its cached disposition. Supplied cases independently cover malformed initial data, baseline/current control occupancy including ig/il/il-consumed/rs/rt, and poison plus otherwise deferrable references. Control/refusal ordering precedes reference deferral for a baseline-absent actor; baseline-present actors remain an explicit existing path, not lifecycle adoption.

7. Handle, phase, failure and transaction boundaries hold for the tested genuine paths. The supplied unchanged groups exercise nil/copied/foreign handles, closed registries, invalid role/side, missing or declaration-carried hint, mixed revision, early freeze, nested registry invocation, ordinary parent refusal, body error/panic, unregistered facade mutation, real oversized-value failure, actual ErrTxnTooBig and an actual Badger commit conflict. Structural/storage failures poison even when caught; failed scopes return zero reports and preserve owned rows/events, with the competitor's committed row retained in the conflict case. Subsequent transactions progress after failure. The in-memory custom-handle capacity fixtures induce real Badger limits; they do not establish a multiple-root coordination guarantee. The observer-panic test explicitly proves postcommit persistence and zero returned report through panic; it does not assert rollback of an already committed transaction. This matches the existing coordinator's stated exclusion.

8. Reports and decisions own mutable data, and registration itself has no graph side effect. Supplied tests mutate caller revision/observation buffers and returned decision data, and later mutate report bytes while checking private/store state. My independent tests additionally mutate deferred observation projections, fact before-images, baseline entity alias/repo-ref slices and raw rows, final entity alias slices, writer entry images and final-image maps, and fact reference maps. Other report projections and owned internal history remain correct. The competing-link test checks no alias between copies of one occurrence linked to multiple candidates. Registry register/link methods have no storage calls; only explicit private writer methods delegate to owned ledgers, and ordinary facade episode/queue writes retain existing transactional semantics. Report position and fact ordinals are accounting labels.

## Strict finite exclusions and future policy obligations

A registered unsupported identity can still commit: the supplied exact-derivation case materializes Atlas with no fact support and is expected to pass this accounting contract. Value-like names can still be registered as proposals, as my original-side test deliberately demonstrates. No status-value boundary, artifact guard, semantic identity selection, alias ownership, support oracle, undo relationship closure, B evidence, immutable observation persistence, selected generation, or production lifecycle is established. The independently tested competing mention links are proposals, not routing certificates. Any future consumer treating these handles or links as sufficient approval would need a separate reviewed policy.

Fact ordinals can exceed the revision's fact list; the supplied 999 and independent 8675309 labels are deliberately descriptive. The accounting facade can delete a baseline fact while preserving a deferred observation of it. These are future authorization/preservation obligations, not permission for production deletion or arbitrary fact admission. Existing baseline actor updates and alias claims remain mechanically recorded without new semantic authority. Old alias/attestation data does not establish an identity, and post-undo alias closure is not certified.

Prior memory-only alias proposals, transient raw writes not in the owned history, other uncoordinated raw Badger writers, forged private state, concurrent or escaped facades, captured-root synchronous writes inside serialized callbacks, and the existing coordinator lifecycle exclusions remain outside the guarantee. Exact recorded coverage plus verified final inventory is not a universal writer audit. A raw transient operation can return the same final bytes and escape final snapshot detection; future all-producer routing must gate entry points with issued handles. The original tests and documented coordinator counterexamples remain untouched.

No EP provenance promise follows from parsing the revision. My independent accounting case commits en/fa with no matching ep row or ig generation row, as permitted by this private unit. Future durable observations/results must establish actual immutable EP identity/time through the reviewed provenance path. Identical Force produces existing-not-new here; retention and selection of earlier birth inventory remain the separate complete current-result policy's responsibility. This report does not establish the original live-goal clauses, recall/latency, production prevention, restored-replica suitability, binary identity, cleanup, or two-sweep stability. No integration may silently reinterpret this bounded GO as any of those approvals.

## Pins

Paths without a prefix are relative to `/tmp/scry-birth-code-disproof.VmQqTj`; Go source filenames below are in `internal/memory/store/`.

| Artifact | SHA-256 |
| --- | --- |
| External frozen BIRTH_REGISTRATION_CONTRACT.md | e512d77638a6667f150db4f2aa19c33709a6acfccd87b44f90e1f46624288f15 |
| External frozen BIRTH_REGISTRATION_DESIGN.md | 19474eeb08d39c5f9b22ee4cade609bec54bbd29518533321a6ee24f265855ed |
| External complete BIRTH_REGISTRATION_DESIGN_REVIEW.md | cd07b5b47183a15764f413868cea9e27a69f3342a81099eeab8264f4e458075c |
| docs/memory-repairs/serial-admission-code-independent-review-2026-09-06.md | cc5b969782b03b8fdaf6411f2d691a2e1e8955e6c4d4899e8c0aafaec3cd657f |
| identity_birth_registration.go | 509faba40f6e3f56710d2475b50bddd8091375decff5d80e7cb3aca6147ccb02 |
| identity_birth_registration_test.go | ffd97200bc267672be00bd938046ef84acb9c67758d7875494b49fede775410b |
| identity_birth_registration_boundaries_test.go | ef5339c7dfb787276d137df0d77dc22ec7c3748077090678fd8f07e854a71fce |
| identity_birth_registration_storage_test.go | 6085a0ec28947cfe7b2d552bd76967b11f3ece9b4daab120034b62176b9f34fc |
| birth_code_independent_test.go | 61dab1aa8f7b857a81852f0ee1e05a292c400848e04df0678f6551428a21013a |
| identity_input_revision.go | b69e6daf91e98fba34166fe949c374d65f9196831d49ddcd11b9650364e59908 |
| identity_mutation_ledger.go | e43d2b90840774ea732a8b22299da5fd3b919d8722b2e7f4525d65e8401d2767 |
| identity_fact_ledger.go | 2040e19a3e0bcf505b6faf8253551e687f6d4dc2059823aeeb744536cb8697c7 |
| identity_writer_history.go | 25022d38ec62f80a4eb1ef1759938c2e651c0f8ed864e563ce40ad092eb2d148 |
| identity_generation.go | e07c6c50affce548b31a94dc2e94d665a49ccab1d246f9433f49277535619592 |
| identity_observation.go | 83a39c03435185963a4a2931f94f30f03a7bd1a86c6e4c545ece95580ad9e0ab |
| identity_admission_owner.go | 396389b06177ce1cd4af18f23412eea4e1f747696d93fc794d82025bfc89dac1 |
| store.go | 8093c39a9214e1dbe5546a8cdd5a8cd8c76836952bf2a8616630eeda3ba8f7b1 |
| birth-code-targeted.log | 2f0bedc53c69d3bc0c80a44119b13692a10abe22797eec3d80e43f0fa8c4dd04 |
| birth-code-fullsuite.log | 33dbbcbf262da6fa05f90e6d70933fa213b99b450462c9d3dc4b8414c17678f1 |
| External initial builder failed log | 7afef8d679b84c597fb0a48e6eabb34913f80814f37762baa68c37b4395fb2b7 |

External contract/design and initial builder log are in `/tmp/scry-birth-registration-sep06.vZeC2U`. External design review is `/tmp/scry-birth-design-disproof.uWc8AE/BIRTH_REGISTRATION_DESIGN_REVIEW.md`. This report's own hash is supplied separately. The lead must read the complete report before citing its bounded verdict.
