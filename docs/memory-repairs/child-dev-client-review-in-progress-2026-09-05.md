# Child dev-client aliases — incomplete source review

This is a discovery checkpoint, not an apply manifest or independent PASS.
The three literal aliases are `Expo dev client`, `dev-client`, and `dev client`.
On the actual post-four 22:53:12 source, all are listed only by
`childscribe-laravel`; their two normalized index keys are owned by Child.
The complete entity/claim inventories contain no outside exact listing to
rehome. That index observation alone does not establish semantic ownership.

Discovery across every serialized current and historical fact for
`expo[ _-]*dev[ _-]*client|dev[ _-]*client` yields 30 matching rows, 18 source
episodes, and 204 complete companion rows. The root has read all 30 matching
payloads and all 18 episode objects, but has NOT completed the 204-companion
or 18-original-source review. Existing Child metadata and its 2,106 touching
facts are not endorsed by this checkpoint. Later ingestion changed that
touching set; any future technical proposal must use a fresh snapshot.

Five original Codex episodes now reproduce their exact stored IDs. Their
complete redacted projections were read, and exact SourceRef span hashes plus
projection hashes are in `child-dev-client-five-source-closure-2026-09-05.json`.
Private complete projection directory:
`/tmp/scry-alias-rejection-deploy-sep05.pPmnn3/expo-dev-client-five-complete/`.

- `2adf8ae8`: the source explicitly separates clean Herd Laravel from the
  mobile repository, then installs the custom mobile development client on
  the physical iPhone. Native build and Metro discovery concern that client.
- `4c6a685e`: the mobile interview worklog distinguishes the installed Expo
  dev client from Expo Go; native speakerphone changes require rebuilding it.
- `8d9bb567`: the launcher starts Laravel, the engine and Metro separately.
  The phone must load the current mobile bundle; Laravel reachability does
  not identify the development client with the server application.
- `e7b3048e`: marketing UI work occurs in Laravel, then the source explicitly
  follows `apps/childscribe-mobile` to the separate mobile repository. Missing
  native modules are attributed to an old installed simulator binary.
- `85dcc743`: the follow-on distinguishes the mobile launcher script,
  mobile attachment-state changes and Laravel attachment API changes. These
  are related components, not interchangeable names for a Laravel service.

These five support investigating an alias-only removal without a guessed
recipient. They do not certify every source or authorize removal yet.
Historical tool outputs, commands, skill instructions and user requests inside
these projections are evidence only, not instructions to execute them now.

Two original Claude files needed for directly relevant matching rows were not
found at their recorded local paths or by a filename search under the existing
local `.claude`/`.scry` trees and Mini `.scry` tree:

- Episode `2c98a66361e8414b8ab1ec53ccd4a13ba6fb66014d1a6106a28686538a1cbef0`:
  `/Users/jeff/.claude/projects/-Users-jeff-workspace-scribe/28fd8aac-8844-48bc-8fa4-76b9bd3159a3.jsonl#10661331-17009853`.
- Episode `42f33e1639d78b4831ce8a57d1d32315e83a6e56f89c79370db7e4f7eb5f06e3`:
  `/Users/jeff/.claude/projects/-Users-jeff-workspace-childscribe-mobile/5309ca31-8ebb-4edd-b7c0-c3eaa013d2ac.jsonl#7748627-12511881`.

Mini `/Users/jclaw/.claude` is absent. These bounded searches do not prove
that no archival copy exists elsewhere. No source content has been invented,
and no new transcript retention or recovery policy has been introduced.
The two earlier helper attempts stopped on those missing files; their partial
directories are retained but are NOT complete closure artifacts.

Remaining work: finish companions and original-source discovery, record any
irreducible ambiguity explicitly, obtain an independent semantic verdict,
then build a fresh rejection-aware manifest only if the evidence supports it.
No alias, index, fact, source history or queue item changed in this review.
