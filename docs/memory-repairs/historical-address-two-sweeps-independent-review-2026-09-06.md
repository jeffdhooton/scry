# Independent a06cd7b two-sweep and later-ingestion grade

2026-09-06 UTC. **Whole goal FAIL. Bounded preservation of original assertions, historical records, existing claims, repair markers and queued inputs PASS.** The captured intervals contain a newly created entity with zero facts. Aggregate zero-fact counts later return to their starting value because a different old hollow entity acquires facts; the new husk remains. Original recall floors and hygiene convergence also remain failed. Zero CLI sweep errors do not certify successful extraction, a clean graph, or the complete goal.

This fresh-context grader ran `scry memory orient --cwd .` before other work and read the supplied AGENTS instructions, the complete active goal at `/Users/jeff/.codex/attachments/4eb02e47-fa9c-4a97-a378-3e707c35e8e2/goal-objective.md`, and the complete actual-deployment review. The latter independently hashes to `227f66914659b8007050c0cfa9656154b9b6f0d036a6638cd8f04edea708cdd6`. Earlier reviewer conclusions are context, not a substitute for the checks below.

All work and evidence are in `/tmp/scry-historical-two-sweeps-disproof.ya0M0I`. The grader exported `git archive a06cd7b` there, read reused helper files completely, and installed/adapted helpers with apply_patch. No shared source edit, live write, provider call, retry, sweep, deploy, restart, or configuration change was performed. Hygiene calls were explicitly dry-run on private restored snapshots, with complete raw maps verified equal afterward. Private databases contain real data and must not be published.

## Artifacts, receipts, and chronology

An independent exact-export build using `CGO_ENABLED=0 GOOS=darwin GOARCH=arm64 go build -trimpath -ldflags '-s -w -X main.Version=a06cd7b'` reproduces SHA-256 `7938d05258bb3b08461441b7448a8da4a95374664f42a9b240f22c3327de55ef`. Both installed laptop/Mini binaries hash to this value. Both retained `.pre-a06cd7b-20260906T0250Z` artifacts independently hash to `7783216755045eb365e0e0cd281ead8e2842da1b8fb697dfb42158c232cbb7e7`. Read-only `ps` still identifies the expected installed executable paths and daemon starts at 02:54:47/48 UTC. This is installed-file/process-path evidence, not a resident-memory cryptographic measurement. The Mini originals of all three shared backups independently hash to the corresponding complete downloaded files.

The independently read, root-generated CLI receipts are `/tmp/scry-historical-deploy-sep06.INlBVW/first-explicit-sweep.log` (399 bytes; SHA `5642fae591992f097f3aae6d5a151cace3c7dc7470916e55c32c87e5c3808424`) and `second-explicit-sweep.log` (345 bytes; SHA `60df0eae0a08020c3b8c291dcb3748357a122b7bb049df82e30e8b8b917fe27e`). Each includes its command-clock start/finish, installed artifact hash and actual CLI counts:

| Receipt | Start–finish UTC | Scanned | Ingested files | Episodes | Errors |
|---|---|---:|---:|---:|---:|
| First | 03:02:43–03:04:10 | 2398 | 7 | 11: Claude 6, Codex 5 | 0 |
| Second | 03:06:20–03:08:23 | 2398 | 0 | 0 | 0 |

The first formatter incorrectly computed lowercase `.errors`, producing a null derived count. Its raw actual `Errors` field is null, which independently establishes no error array entries; the derived null is not used as the proof. The second formatter records zero correctly. These are independently verified contents of root-generated receipts, not invocations independently observed by this grader.

The first post-sweep snapshot's persisted last-sweep report has already been replaced by a different, 94-file zero-ingestion sweep completing at 03:04:15.185246 UTC. It cannot independently prove the first 2398-file invocation. The second snapshot's persisted report records 2398 scanned, zero ingested/episodes/errors, finishing at 03:08:23.320361 UTC, and its separate last-sweep timestamp matches exactly. Persisted reports establish completion and last-writer state, not CLI start times. Background extraction continued after CLI completion; snapshot deltas are interval evidence and cannot all be attributed to the CLI invocation alone.

## Six complete backups and active logical records

Every complete file was hashed before direct Badger Load into a new, previously nonexistent private destination. No populated application Restore was used. Each replica's entire active key/value map is byte-identical before and after Store.Open, AllFacts, Entities, offline index construction and bounded recall.

| Store/snapshot UTC | Bytes | Complete SHA-256 |
|---|---:|---|
| Shared 02:55:09 | 75,362,922 | `2f60f448e8d3844e6fd2692aa69891a693e6cd41145e2c47fac336af8ed7743b` |
| Shared 03:05:43 | 75,539,351 | `969a6a7eaa3e430aa319898f120b3268d8e89adb048a180c87a9d586bbdd4dc5` |
| Shared 03:10:13 | 75,615,114 | `dcdfd142a21fa3cda6759aa54cd16d5619e19dd4e59553fb31e93e1fe4ea5ab6` |
| Laptop 02:55:09 | 19,445,024 | `9a65e98f7a841cb90c17a2f8ba5026be32edf488846dc291e151d98da78655fa` |
| Laptop 03:05:43 | 19,445,016 | `3f6c887c40c318bbacfab2492f55d9de59ceb4222ef5d6098178d6fa2bdaa5ae` |
| Laptop 03:10:13 | 19,445,000 | `83a302b8c3d87db6ce6ee545d9637f60cd2df4c1937e843c1387ce34aeb672e6` |

Shared input paths are `/tmp/scry-historical-deploy-sep06.INlBVW/memory-20260906T<time>Z.badger`, with originals under `/Users/jclaw/.scry/backups/` on Mini. Laptop inputs are under `/Users/jeff/.scry/backups/` with the same filenames.

All 83,378 laptop active records remain raw-exact across both adjacent intervals and the full interval: logical digest `8efead71b3128363e95c7f39678e3ec9332ae4a7645af03454ccf4780f02c2c7`. Its 21,004 facts include 18,495 current and 2,509 historical. Every family and every independently computed structural defect set is unchanged; this does not certify a clean laptop graph. It retains 8,820 current facts outside the shared canonical vocabulary. An initial summary assertion incorrectly applied the shared zero-noncanonical bar to the laptop and failed; the corrected summary scopes that bar to the shared store while retaining full laptop byte-preservation checks.

Shared logical digests are `e88e1d8a5fb5c922ecd028393e2a09ba2bcdb20eecc383f6c6d3ef750c5bf720`, `d973f30202f02629a2a4a17115ab649ffd199a6b998f32f5257b703902df0f2e`, and `fd03634f550aca09bc7ba5c9fb62ab6c0e8321faad8e0102873d27662b8a9c92`. Complete family comparisons retain every active key, including unknown-family handling; no selected-family export or typed reserialization substitutes for the raw comparison.

| Shared family | Baseline | First | Second |
|---|---:|---:|---:|
| adjacency | 56,227 | 56,256 | 56,302 |
| alias claims | 52,570 | 52,593 | 52,663 |
| alias rejection markers | 4 | 4 | 4 |
| attestations | 11,146 | 11,159 | 11,187 |
| cursors | 3,163 | 3,168 | 3,168 |
| entities | 31,074 | 31,089 | 31,133 |
| episodes | 9,425 | 9,428 | 9,434 |
| facts | 81,514 | 81,555 | 81,634 |
| metadata | 5 | 5 | 5 |
| pending | 23 | 31 | 26 |
| retirement spellings | 19 | 19 | 19 |
| retirement targets | 19 | 19 | 19 |
| value evidence | 1,030 | 1,051 | 1,096 |
| Total | 246,219 | 246,377 | 246,690 |

All family added/changed/removed/equal counts and individual changed-row hashes are recorded separately for both adjacent intervals and the full interval in `*-delta.json`; drift, queue, closure, supplement and audit files independently extend those checks.

## Preservation and actual mutations

All 81,514 original shared facts remain at their original keys with exact assertion text, endpoints, value, raw relation, ValidFrom, and confidence; every original provenance ID remains. There are no removals, relocations, original start changes, or confidence decreases. All 7,863 baseline historical records are byte-identical, including InvalidAt and any unknown JSON fields. Historical count becomes 7,865 then 7,870 as ordinary ingestion adds historical facts and closes current ones. The a06 contract permits exact historical-address evidence union/max-confidence updates; this audit explicitly supports that contract, but the captured historical rows required no such updates.

Exactly three baseline current fact payloads change: two acquire InvalidAt and one gains provenance. Both invalidations are no earlier than their retained ValidFrom. The audit does not describe current validity timestamps as entirely unchanged. The resolver's inherited current coalescing/backdating semantics remain outside this bounded certification: this particular chain contains zero backdated original starts or relocations, so no timestamp changes are hidden as byte preservation. Newly extracted assertion semantics or the intended truth of invalidations are not independently certified by structural closure.

All 52,570 original alias claims and 42 rejection/retirement markers remain byte-identical. No original entity or episode is removed. Existing entity names, slugs and creation times remain exact; no concrete type or nonempty description is replaced. One empty description is filled with 141 bytes and one concept is promoted to runbook, both matching the explicit guarded enrichment code at resolve.go. The initial strict all-core-fields-equal drift check failed on these changes; its binary and partial evidence are retained. A separate enrichment-aware continuation reports the differences and validates the actual guards. It does not turn literal metadata equality into PASS. Existing aliases and repository references are retained in this chain.

All 23 baseline parked complete inputs remain byte-identical through the final snapshot, including the inherited parked fact conflict. Two new inputs are parked with alias-claimed errors, one attempt each, no committed episode and no citing fact. A third newly queued input remains nonparked after one deadline-exceeded attempt. The first-to-second interval removes five pending inputs; each has a committed episode with exact ID/source/sourceRef/occurredAt/cwd fields. The remaining input changes only attempts, error and next-attempt fields; all nonretry input and unknown fields survive. The first snapshot does not contain the just-enqueued originals of the two newly parked inputs, so their pre-enqueue payload equality is not independently measurable. The grader never retried any input.

All 120 added facts have existing endpoints, nonempty provenance closed to committed episodes, and at least one episode absent from baseline. Added/changed value evidence preserves prior spelling/episode sets and has closed provenance. All fact provenance and reverse adjacency checks report zero missing entries; all reverse adjacency counts match their fact keys. Alias attestation provenance has 18 missing episode references already present at baseline; the exact defect set is unchanged, SHA `d169a324074952da3f47c3572d033fe0a44f457145c48b2cb44dc39b18b5ce67`. This inherited attestation defect must not be conflated with clean fact-provenance closure.

The actual deployment note supplied by the lead is absent from baseline/first snapshots and committed in the second, with no remaining pending input. Its occurrence is 03:06:20.723758 UTC and ingestion 03:09:13.687952 UTC; 15 facts cite it. Complete episode payload SHA is `a648c31778ef2ec1f314619c1ad0665f79dcefac0ca6117d8a7fbad8f7e21d0b`. This independently proves durable episode/fact closure. The lead's claim that the call was submitted once is not independently reconstructed from the single durable episode record, and this grader made no submission or retry.

## Structural counterexample

The first interval creates a true zero-fact runbook, entity-key SHA `dc577a2509acf3dc60664032c31fb2285079da8d1f7042a5678e277a50b001b6`, defect SHA `47118b30769cbed94ac61058461ad6a14bc53cb1037180b3c2c1b70f2bff9120`. It has zero current and zero historical touching facts, a 174-byte description, one repository reference, and a correctly owned name claim. Two attestations identify the same newly committed episode; that episode ingested at 03:05:32.914771 UTC. Its entity creation timestamp equals the episode's earlier occurrence at 01:46:07.876 UTC, not the store-write time. That earlier timestamp does not establish predeployment existence: the entity is absent from the full baseline raw map.

The new husk remains at the second snapshot. A different old zero-fact entity, defect SHA `df1912786a1008385da3992614e8d95a5a0a0e48fa3a10d933169148f9e158a3`, acquires facts in the second interval. Thus no-fact counts are 2854→2855→2854 and no-current counts 2975→2976→2975, with one added and one removed defect across the full interval. Equal final aggregate counts would conceal the new defect.

Proper Name+Aliases claims, excluding pure slug-only listings, retain their exact defect sets on every snapshot: 504 wrong-owner occurrences, 29 unlisted owner claims, 462 multiply listed spellings, 27 cross-type spellings, zero missing listed claims and zero dangling claim owners. Every one of the 93 newly added name/alias spelling checks resolves to its expected owner. Current relation vocabulary remains exactly the documented 39 with zero noncanonical current facts. Dangling fact endpoint occurrences remain 2441; self-loops remain 1099 total/93 current. No new member of those defect sets appears. This is bounded nonregression, not the required zero-defect state.

## Retrieval and unfinished bars

Fifteen actual installed-CLI benchmark commands run the five unchanged benchmark files against all three shared snapshots. Independent recaller runs additionally record all 235 question SHA/rank/payload-size/full-response SHA tuples per snapshot. Every individual response is below 24 KB; raw maps remain exact after every retrieval pass.

| Suite | Hits, all snapshots | Max bytes baseline / first / second |
|---|---|---|
| heldout-2026-09-03 | 51/62 | 12111 / 12109 / 12110 |
| heldout-b | 29/66 | 13359 / 13360 / 13370 |
| probes | 7/7 | 9974 / 9974 / 9972 |
| tuning-strict | 45/50 | 11542 / 11545 / 11544 |
| tuning | 47/50 | 11542 / 11545 / 11544 |

The original 53/62 and 34/66 floors remain failed. Complete response hashes differ for all 235 questions across each snapshot interval, consistent with changing ingestion/index payloads; no cross-snapshot response byte equality is asserted. Individual rank changes are 1 baseline→first, 1 first→second, and 2 baseline→second. Question membership is identical. All measurements and full-response hashes are retained in the benchmark/rank files and summarized with assertions by `summarize.cjs`.

Independent frozen dry hygiene is not a no-op: all three snapshots report 487 cross-type collision pairs, 305 proposed entity changes, 114 alias drops, 295 splits, 296 reattachments, 93 self-loop invalidations, 17 stub-claim drops and 32 stub merges. Its collision definition includes additional spellings/pair semantics and differs from the independent 27 proper Name+Aliases cross-type spelling count. No proposal was applied. Final dry-hygiene measurements are recorded in `shared-second-hygiene.json` and the checked summary; proposal sample hashes are not assumed order-stable or equal across snapshots.

This grade does not certify the fresh fifty-question recall bar, semantic status-value admission, remember p95, orient coverage, historical recovery, the complete merge/repair manifests, a hygiene no-op, or two all-clauses-passing rounds. Existing failures and the new zero-fact counterexample disprove the whole goal independently of those ungraded clauses.

Evidence concerns active logical Badger records, not physical SST/vlog equality, deleted/expired rows, hidden older versions, or crash recovery. Complete private replicas and the initially failed strict check are retained. `ARTIFACTS.sha256` pins this report, sanitized evidence, helper sources, scripts and built binaries.
