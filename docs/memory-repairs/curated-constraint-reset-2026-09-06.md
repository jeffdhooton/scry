# September 6 reset: one curated constraint

The user’s September 6 reset supersedes the admission goal. The September 4
workflow assessment is dated evidence of the curated-memory gap, not a backlog.
This run delivers only an opt-in source-to-orientation workflow, locally.

## Preserved before implementation

Source baseline: `36dc1e5e42328adaeb7baf5035e8e7def5ae07b5`; no tracked edits.
The pre-existing untracked assessment and goal document are retained unchanged.

Durable local archive:
`/Users/jeff/scry-local-archives/curated-reset-20260906-8r5n6vsg`.
Both complete private directories were copied into new, previously nonexistent
destinations. `verification.json` records original-before == original-after ==
copy for all paths, types, modes, file sizes, SHA-256 hashes and symlink targets.

| Archive | Entries | Manifest SHA-256 |
|---|---:|---|
| `scry-complete-admission-sep06.6qYb7K` | 946 | `bdd82da0d368290b1bc24fc3063be5d98c7c4ce71d008e0758c2a1b1de9d3b26` |
| `scry-owned-corrected-sep06.7Aah7P` | 780 | `0473cc52585d23a5a2d70de7d40a1584e42e3b7d29abca65f7f9205279165d9a` |

The archive includes their source, tests, logs, freeze manifest, review reports,
and a copy of repository memory-repair evidence. These are historical results,
not current reruns or fresh independent approval. Missing historical evidence
is not reconstructed. `known-binaries-backups.json` inventories 208 explicitly
located/referenced paths (182 present, 26 missing locally), including installed
and rollback binaries and known backups. Files were hashed, never executed or
opened as stores. Remote references are historical, not remotely reverified.

## Reset inventory

| Category | Evidence and disposition |
|---|---|
| Delivered repository code | Transactional ordinary resolution, alias ownership safeguards, repository attestation, deterministic queue parking, fact collision refusal, historical restatement preservation and schema startup refusal remain in the baseline. Retain these safety changes. Existing uncalled admission foundations stay uncalled. |
| Private experiments | Complete admission candidate and corrected owned freeze above. Neither is promoted or used as the implementation base. Earlier passing component reviews cover only their pinned scopes. |
| Confirmed private regression | Adapter omits durable explicit value-evidence writes. Archived `normal-adapter-value-evidence-first.log` shows later recreation of `dirty-working-tree` / `readiness-signal` and loss of later supersession-hint utility. Keep the failing tests and counterexample. |
| Confirmed historical live observation | The archived schema-refusal two-sweeps review reports a newly persisted zero-fact entity after deployment. That dated graph-quality regression is not a startup-refusal failure or a new local baseline failure; it was not remeasured or repaired in this run. |
| Unclassified failures | Archived `normal-adapter-legacy-compatibility.log` has exactly 71 top-level failing groups; `historical-failures.json` retains every name and its log hash. The run completed in 13.264s with failures, not a timeout. Do not classify the remaining failures as obsolete tests. |
| Deferred | Admission integration/redesign, remaining 71-failure classification, value-evidence prototype repair, adoption/writer-floor/rollback design, old live recovery and benchmarks, hooks, broad curated crawling, model-quality evaluation, embeddings and the excluded 41-group consolidation. None is an acceptance gate for this slice. |

The candidate’s later value-evidence notes are an unfinished implementation
proposal, not proof that the counterexample was fixed. The normal-integration
progress report’s private source pins describe its earlier moment; the archive
manifest describes the exact bytes present at this reset.

## Acceptance checklist

- [x] Exact private artifacts and historical evidence archived and verified.
- [x] Reset inventory and untouched binary/backup inventory retained.
- [x] Baseline full tests and vet recorded (`baseline-results.json`).
- [x] Explicit file and repository mapping through durable ingestion (`TestCuratedSelectedRepositoryFile`).
- [x] Fresh-client orientation after queue processing and store reopen (`TestCuratedConstraintDurableWorkflow`; final run uses the built CLI).
- [x] Edit, retained history/export, unchanged replay and failure/cursor evidence (same E2E).
- [x] Unrelated repository isolation and existing safety regressions pass (`checkpoint-safety.log`, full suites).
- [x] Final focused/full tests and vet, timings, outputs, limitations and opt-in procedure ([workflow evidence](curated-constraint-workflow-2026-09-06.md)).

The corrected freeze's existing manifest was also independently rehashed by this
run: `4d8593e4443909eeadc2da88711207704a5f3c5c3a6896b20ee4ed2780796f58`.
All 600 baseline files and 107 additions match its pins. This is a local hash
verification, not a new independent semantic review of the admission prototype.

Final full tests passed in 37.50 s (unchanged packages may use Go's test cache);
vet passed in 0.95 s. The earlier checkpoint full suite also passed in 158.63 s.
There were no new or baseline test failures. Historical private failures above
remain unresolved and excluded. The final source/patch, build, manifests and
command results are retained in the archive. Stop for review; no next milestone.

No live-store mutation, repair, restore, deployment, daemon restart, hook/config
change, hosted-provider call, install, push or deletion is authorized in this run.
