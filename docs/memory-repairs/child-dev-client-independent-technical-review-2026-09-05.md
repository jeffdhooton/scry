# Independent fixed-source technical grade: Child dev-client alias removal

Bounded verdict: PASS for the exact three-literal, two-key proposal against the complete 2026-09-05 23:51:34 UTC backup. The operation removes only Child's `Expo dev client`, `dev-client`, and `dev client`, preserving owner-specific rejection evidence. No rehome, fact correction, current-live freshness, apply approval, broad alias review, cleanliness, or whole-goal acceptance is granted. The lead reports later live ingestion; this review deliberately does not grade that later state or the lead's subsequent code edits.

All reviewer work is under `/tmp/scry-dev-client-technical.QyrtR5`, an independently extracted archive of deployed source `d1f0a958608389a385ac9a9f57ec1eb941015a10` plus private helpers. No shared source edits, live writes, deployments, provider calls, remembered notes, rooms, queue operations, or external product interactions were performed. The goal explicitly authorizes this fresh-context disproof grade. I ran `scry memory orient --cwd .` first and read the supplied instructions and full goal.

## Exact inputs and semantic boundary

All SHA-256 values below were independently computed. Input files share parent `/tmp/scry-alias-rejection-deploy-sep05.pPmnn3`:

| Input | SHA-256 |
|---|---|
| `memory-20260905T235134Z.badger`, 73,975,028 bytes | `7680cdcff3563c4cc2d33577506fc4c4aad4931d8bae1016f90e617bd7ee863e` |
| semantic original `memory-20260905T233353Z.badger` | `907b9ae1e3210ac3840271e66ab95e34c9a24838863031812937c161bd076703` |
| `dev-client-replica-235134/manifest-replica-only.json` | `0f1c0555d75429da8c9141ccfcb7a5b7e274d2f9ae6a18f51d28870002cbff89` |
| pinned `scry` binary used for all ten CLI controls | `290a14c04ef0cfa9618db3f1a848bc6f3a343ec28d9c197ea49720eedb30b553` |

I read the full root semantic proposal, the complete five- and seven-source closure files, and `docs/memory-repairs/child-dev-client-independent-semantic-review-2026-09-05.md` (SHA `88ca723742562cead2387686a871ea74339ded388d2148278de6dd62c97bd87f`). This grade uses that separate semantic review's bounded negative; it does not misrepresent the technical grader as having rerun its complete twelve-original semantic/distiller examination.

I independently restored both complete backups directly with Badger Load and iterated every logical key/value, without family filtering or using the builder's replica. The source contains 243,703 keys. Against the earlier semantic source, exactly six keys differ: the three intended stops-table repair keys, two sweep metadata values, and one new pending manual CADFormats note. I read the complete decoded delta. The note concerns an unrelated workbench release and remains pending; it introduces no new entity/fact/episode in this snapshot. Child's only change is removal of `stops table`; every fact and stored episode is unchanged. Complete decoded delta: `evidence/source-delta-full.json`.

Independent complete-fact regex discovery reproduces 30 matching rows, 204 one-hop companion facts, and 18 stored episodes. I read their complete stored payloads, the selected potentially confounding entity metadata, and the complete prior semantic report. All independently selected arrays are structurally equal to the provided discovery files; the provided old/new discovery files are physically byte-identical at hashes `0742e21dfbd1aa4da297a49d076dfad2ada7e172dc7af68e693af5c9a1d537f0`, `2a2dc6289c57347d2f32356b1b8a7d8200a17801e99d9f9f4f1c1d47981555d1`, and `9fa75de33835b9f362107a8e5078f4cf6372611887df8ab8aaa3ef02bc6d9aa6`. My JavaScript JSON exports intentionally have ordinary HTML characters where Go exports escaped them; their physical hashes are not falsely called identical.

I rehashed every exact raw range for the twelve available originals, rehashed the prior independent reviewer's complete projections, matched each SourceRef to the restored stored episode, and rehashed the separate Android seed. All supplied sizes/hashes match. The five other original paths still fail with ENOENT; their claims remain unverified. Production distillation was not rerun in this technical grade. `evidence/source-checks.json` states this explicitly.

The negative remains limited to Child. Existing graph assertions attach native/ADB/client facts to Laravel, and the package `loads` assertion points at Child; they are preserved, not treated as positive ownership evidence. Metro's conflating description, the package's `installed client` alias, contaminated Program Health companions, reversed simulator relation, unresolved import/native-build assertions, zero-duration historical rows, missing historical endpoint metadata, Child's false survtest description, and unrelated repo refs remain unchanged. No client/package/mobile-project/Metro/script identity is selected as a recipient.

## Independently predicted transformation and preservation

The full raw entity/name/slug/alias scan and separate repo-reference check find only Child listing either normalized key; both existing index claims name Child. No outside exact listing can be stranded. I independently reconstructed every Expected field, using all 2,112 current and historical facts touching Child, the full entity, exact claims/presence/listings, and all four existing owner rejection arrays. The result exactly equals the submitted manifest:

- Plan `ee37314aab154b99ff1187cf368a6d3a54828723591a491dda80ac8e08aaa438`.
- Entity `e6fb9828420ca61862848900693f7d69ce7c4c51753e15ff3d0fecf1c45f869f`.
- Touching facts `7e8b10b870e25afdf3bfb55912ae71f8dbf83e715000e5eed29db08973ceafec`.
- Rejection closure `f9d75a67575d273a2c9a7df519e0d29a543be121d34161d1b5ef20ecaab517f9`.

I predicted the complete after-map independently before comparing it with the actual private result. Exactly five keys change: update `en:childscribe-laravel`; delete `al:expo-dev-client` and `al:dev-client`; add their two `ar:childscribe-laravel:*` records. Child's aliases go 39 → 36, retaining every other literal in its original order and every other entity field byte-equivalently under production encoding. There are three new literal records across two new rejection keys: `dev-client` then `dev client` remain separate ordered array entries with exact reasons and plan. The four old keys and four old literal records (`driver-core worktree`, `envoyer`, `office dashboard`, `stops table`) survive unchanged. Rejection keys go 4 → 6; literal records go 4 → 7.

Whole-map equality proves preservation of all 80,844 facts (73,013 current, 7,831 historical), 9,371 episodes, 30,725 entities, 55,769 adjacency records, 10,928 attestations, 3,134 cursors, five metadata records, twelve pending records, 761 value-evidence records, and 19 retired-slug plus 19 retirement records. Alias claims go 52,112 → 52,110. No fact text, validity, time, confidence, provenance, raw relation, invalidation, or endpoint changes.

Canonical compact key-to-base64 JSON hashes: before `0c766cd8dec9d9ea45ee43564cbd6740e30c659d9d7a65a65d032f759c0b4d5a`; independently predicted and actual after `a897ca8f056f1b870574158d46ac2b0d157c198b92f2ff4bc543da1464f8fbcc`. Assertions compare every key/value and presence, not merely hashes or counts.

## Backup, failures, write paths, and convergence

Production Store.Open makes no logical changes after direct restore; production Store.Restore reproduces the entire original map. The first preview is read-only. Actual private `BackupAndRepairAliases` produces a synced, closed, nonempty 70,690,306-byte pre-apply backup, SHA `be631435e7542d15bfb3e535471414b9bfbd329bd0e63f74894519e3edfe42f4`; its independent direct restore equals every original key/value. Physical backup size differs from the supplied backup; logical restored equality is the verified invariant.

The second preview refuses the stale/already-completed request. A second actual backed apply also refuses without mutation; its nonempty post-state backup independently restores/reopens to the exact after-map (SHA `ad13fce584b39263af90c7ccee66812fc6ff13824cf4df5ce4a24e29a5f88fd4`). Neither second pass silently rewrites markers or aliases.

Private failure injections independently change the full Child entity, a current touching fact, a historical touching fact, a present claim's owner, claim presence, an outside entity listing, and an existing rejection reason. Every stale preview/backed apply refuses; complete raw comparisons prove no additional mutation. An existing rejection replaced with malformed empty array `[]` fails closed in preview and backed apply. Backup Write, Sync, and Close failures all prevent mutation.

For each of the three literals, direct PutEntity and ClaimAlias reject with ErrAliasRejected; both operations also reject within AtomicWrite after inserting a rollback sentinel, with the sentinel and every other attempted write absent afterward. Synthetic outside owners can represent each spelling, proving this is not a global ban. Explicit rehome back to Child refuses, including the specific prior rejection diagnostic. Both actual merge directions between Child and each synthetic owner refuse, including the explicit unsupported rejection-inheritance diagnostic. All six merge attempts and all three rehome attempts preserve complete maps.

Two actual normal `resolve.Apply` episodes each attempt all three rejected literals and carry three distinct input facts. Neither episode restores either index key or any rejected literal to Child. Both episodes and all six exact input facts survive with their expected Child subject, confidence and episode provenance; the private probe has 80,850 facts. Every original fact, all six rejection arrays, and all retirement records remain unchanged. This is a normal write-path test, not merely AdmitAlias calls or an entity-only episode.

## Structural inventories and pinned retrieval controls

The complete uncapped before/after defect lists are equal, with identical file SHA `2dd3048449a48e3aefcf32b53c785a42eb92acf8ae87a7e0b9c2c0aa20616ab1`. Both contain 2,441 dangling endpoint occurrences; 3,869 missing listing claims; 505 wrong-owner listing claims; 28 unlisted claims; 462 normalized multiple-owner listings; 27 normalized multi-type listings; 2,849 entities with no facts; 2,967 with no current facts; and 1,099 self-loops. Missing source episodes, missing/extra adjacency, and dangling alias-index owners are zero. These structural definitions are not the broader production hygiene collision definition and do not imply a clean graph.

All ten commands use the exact pinned binary and unchanged existing suite files, directly against separate restored before/after databases:

| Suite | Before → after | Mean answer rank, both | Maximum response bytes before → after |
|---|---|---|---|
| heldout-2026-09-03 | 51/62 → 51/62 | 4.8431372549019605 | 12,110 → 12,110 |
| heldout-b | 29/66 → 29/66 | 5.103448275862069 | 13,369 → 13,369 |
| probes | 7/7 → 7/7 | 1 | 10,095 → 10,094 |
| tuning-strict | 45/50 → 45/50 | 4.511111111111111 | 11,528 → 11,531 |
| tuning | 47/50 → 47/50 | 3.978723404255319 | 11,528 → 11,531 |

Every exact missed-question set is equal, every over-cap count is zero, and all responses are below 24,000 bytes. Payload bytes/latencies are not claimed identical. Complete final direct raw rereads after all ten controls equal the original before and actual after maps, proving the controls wrote no logical data. The goal's original 53/62 and 34/66 floors remain unmet; this is preservation, not full recall acceptance or a new held-out grade.

## Reproduction and remaining gates

From `/tmp/scry-dev-client-technical.QyrtR5`, the completed commands were:

```sh
go run ./cmd/technical-review /tmp/scry-dev-client-technical.QyrtR5/evidence /tmp/scry-alias-rejection-deploy-sep05.pPmnn3/memory-20260905T235134Z.badger /tmp/scry-alias-rejection-deploy-sep05.pPmnn3/dev-client-replica-235134/manifest-replica-only.json /tmp/scry-alias-rejection-deploy-sep05.pPmnn3/memory-20260905T233353Z.badger
node audit.cjs
node source-check.cjs
node bench.cjs
go run ./cmd/technical-review /tmp/scry-dev-client-technical.QyrtR5/evidence /tmp/scry-alias-rejection-deploy-sep05.pPmnn3/memory-20260905T235134Z.badger /tmp/scry-alias-rejection-deploy-sep05.pPmnn3/dev-client-replica-235134/manifest-replica-only.json /tmp/scry-alias-rejection-deploy-sep05.pPmnn3/memory-20260905T233353Z.badger scan
go test ./...
```

All passed. To rerun the mutating harness, use a new private evidence directory rather than reusing its completed databases. The first audit run intentionally stopped on an overstrong physical JSON equality assertion: decoded discovery equality had passed, but Go escapes `&` as `\u0026` and JavaScript does not. The corrected assertion checks decoded independent equality and physical old/new submitted-file equality. No data or production code changed to make the grade pass.

`evidence/` retains full before/old/after raw maps and fact/entity/episode inventories, exact Expected, initial/second previews, complete decoded source delta, five-key repair delta, complete defects, source checks, all failure and write-rejection diagnostics, full ten benchmark outputs, normal-resolver probe inventory, and independently restored backups. `file-hashes.json` pins evidence and private helper sources.

This fixed-source PASS cannot be applied to the later live store or a different binary by inference. Any eventual live operation needs separately reviewed current inputs and corresponding actual pre/post checks. Older marker-unaware binaries remain unsafe writers after rejection records exist; rollback must restore a compatible graph and reconcile intervening data. No broad hygiene apply, other alias disposition, inferred rehome, fact cleanup, queue action, deploy, or whole-goal completion follows from this report.
