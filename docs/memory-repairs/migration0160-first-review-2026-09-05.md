# FAIL — incomplete migration-file identity closure; replica mechanics PASS

Review scope: only the exact three-member replica candidate below. No live store was opened or changed, no deploy/model/remember call was made, and no shared repository file was edited. I independently extracted commit `53fafa91d621190c87245f0b0844270b4fdf44c9` with `git archive`, authored independent tests, and restored my own source and rollback replicas under this directory. The builder replica was not opened or changed.

Pinned inputs:

- Source: `/tmp/scry-childscribe-live-sep05.XlzT6P/source.badger`; SHA-256 `b8fda9a1c446d9f03dd8bc3116d1e49fd6020f7cd0e7d728553087b6ae445197`.
- Manifest: `/tmp/scry-migration0160-sep05.5STYbx/measurement/manifest-replica-only.json`; SHA-256 `73c8bdc347b009af43b66e77f2584d7c3bcd5d2857f509668d116eb1212c43b6`.
- My nonempty pre-apply backup: `results/pre-apply.badger`, 77,105,074 bytes; SHA-256 `36d9d45a9c3f134fb8fd2dd30caa33d3d54291b5789c95891471e022678f3d8f`.
- Actual Docket migration `/Users/jeff/workspace/docket/db/migrations/0160_task_evidence_rules.sql` SHA-256 `a806f8bf5eb9dd42e914b4384f0d77a0aad8edc2ea82755e55d0b49eaf535b61`. Independently matched the blob at the sole file-history commit `a7253c09d85b43aded2ab6c6f623c11d8b8d508f`.

Blocking finding: the same source contains two additional identities for this exact migration file. Their omission leaves the migration identity split and its full-path lookup hollow after the proposed repair.

| Source entity | Stored identity evidence | All touching facts | After three-member apply |
| --- | --- | ---: | --- |
| `dbmigrations0160-task-evidence-rulessql` | Name is the exact `db/migrations/0160_task_evidence_rules.sql` path. Description identifies the migration on main with photo/scan/signature/scale_ticket rules, matching the actual file. Type `machine` is contradicted by the file. | 0 current, 0 invalidated | Entity remains; full-path lookup resolves here and returns 0 facts. |
| `migration-0160-task-evidence-rules` | Description explicitly names the Docket file and says it creates `task_evidence_rules`. Source episode `06139be9…` describes that same implementation in the Docket setpoint worktree. | 2 current, 0 invalidated | Entity remains; its exact name resolves here and returns only its 2 facts. |

The omitted tool's two incoming facts are `evidence-rule-repository depends_on migration-0160-task-evidence-rules` and `task-evidence-and-readiness uses migration-0160-task-evidence-rules`. Their complete text, validity, confidence, and episode provenance are in `results/full-five-semantic-closure.json`. They describe use of the table created by the file; the entity's explicit name/description and episode identify the migration artifact, not a separate table entity. The omitted machine has no fact provenance to reconstruct, but its exact qualified filename and concrete description independently match the actual SQL file. Lack of facts must not exclude a hollow identity from this repair.

Full reviewed closure on this source is five records, six current facts, zero invalidated facts, and seven normalized spelling keys. Full scanning of all entities and every alias-index claim found no owner or normalized/folded listing outside those five for any of their spellings. Every source metadata object and listing/owner pair is recorded in `results/full-five-semantic-closure.json`. The additional full path normalizes to `db/migrations/0160-task-evidence-rules.sql` and is indexed to the omitted machine; its slug has no alias-index entry. The five-record maximum `last_seen` is `2026-08-23T21:45:43.316Z`, later than the three-record proposal's `2026-08-19T05:02:21.522Z`. Earliest creation remains `2026-08-19T02:49:16.995Z`. All five have no original repo refs; the proposed Docket ref is independently supported by the actual file and source episodes.

Distinct controls remain outside the identity: `task-evidence-rules` explicitly names the database table; `task-event` names the API enforcement module; `evidence-rule-repository` names a different API repository; `migration-block-registry` and `order-lifecycle-migration-block` describe reservation coordination/ranges; `task-evidence-and-readiness` is the encompassing implementation task. Their source metadata and complete touching facts are included as controls. The actual SQL creates the evidence-rule table and adds `task_events.evidence_json`; it is not itself the table, enforcement implementation, or reservation task. Additional migration-range records and unrelated StateLicenseLookup 0160 facts surfaced by the broad semantic search are not evidence for merging those identities.

The survivor's SQL-filename name and `tool` classification are supported by the real artifact, independent of fact counts. The reviewed description is a deliberate synthesis rather than a literal concatenation: it does omit the prior description's historical fixture-only authoring limitation. The referenced authoring episode and distinct repository description preserve the authoring context, and explicit reviewed descriptions are permitted by the goal; I do not count this alone as a violation. A regenerated manifest should consciously review that historical context and the omitted machine's later timestamp.

Independent mechanics findings for the exact three-record manifest:

- PASS: all 79,692 current and invalidated facts preserved, including complete JSON content/provenance. Only expected endpoint relocation occurred. Four facts touch the group; two already belonged to the survivor and remain unchanged, two change endpoints.
- PASS: complete raw key/value state equals an independently constructed expected map, including every entity, alias, forward fact key, reverse adjacency, episode, cursor, and other key. Exactly 12 keys differ; total keys 239,692 → 239,691. No unrelated metadata/index/fact mutations.
- PASS: entities 30,231 → 30,229. Both named retirees are absent; no fact endpoint or alias owner still refers to them. All six literal/normalized approved input spellings resolve to the survivor and return four facts.
- PASS: collision count 489 → 487, matching the expected final entity/fact state. Full normalized and hygiene-folded listing scans find no outside claim for this three-record set.
- PASS: nonempty backup was synced and closed before apply, restored into a second new store, and every restored raw key/value equals the pre-apply state. This proves restored logical bytes, not equality of two independently generated backup files.
- PASS: initial preview writes nothing. Deliberately failing the transactional postcondition aborts with complete raw-state equality. Second preview is not ready, reports precisely the two missing retirees, and writes nothing. Attempted repeat apply rejects and writes nothing.
- PASS bounded to no regressions: complete hollow/dangling/self-loop inventories are unchanged. This is not a graphwide clean result: the source and replica retain 2,854 hollow entities, 994 distinct dangling endpoint slugs, and 1,095 self-loop facts. The omitted full-path migration record is one of those pre-existing hollows.

Executable checks passed: `TestIndependent0160` (store), `TestIndependent0160Audit` (resolve), and `TestIndependent0160FullSemanticClosure` (resolve). Evidence is in `results/summary.json`, `results/preview.json`, `results/second-preview.json`, `results/independent-audit.json`, and `results/full-five-semantic-closure.json`. The independent check sources are the two `independent_0160_test.go` files in this temporary archive.

Disposition: FAIL for presenting the current three-record manifest as a complete migration identity repair. The primitive's bounded mechanical replay passes, but the exact qualified path still gives an empty answer and two file facts remain split. Regenerate and review the explicit five-record semantic closure, with full identity metadata/spelling preservation, against a newly stable post-ChildScribe source. This review does not approve any expanded manifest, future live batch, or live apply of the stale source.
