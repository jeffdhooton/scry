# Ninth ordered identity overlay — independent bounded disproof

2026-09-06. NO-GO for the exact private, uncalled, read-only planner. Two fresh fixed-entry cases demonstrate that the generation-vote path consumes an empty stored-attestation query but returns no scoped absence evidence for that query. Initial and cached decisions are both affected. The ninth FA reference correction passes the retained eighth disproof and the new independent reference matrix; it is not the source of this finding.

Reviewed source: `/tmp/scry-ordered-overlay-ninth-sep06.nAculd/code`, baseline `e097fa6`, freeze SHA-256 `2e06afc117db9c961924dffc93202f816b2f9523e53a29997c364c6195247dbb`. Input SHA-256 `025375d9a8d6c84071156c8181ddb9a8a22ffec1b4bbab2d2ed0bdcb883e6dcc`; controlling complete contract SHA-256 `e56d63aa5b3461b8a4416f32697e9477f4afdd040d15e5c0e4e3cbcf099856ef`. Initial and final verification preserve all 600 baseline SHA-256/git-blob pins and 54 addition SHA-256/byte-length pins in BOTH frozen and independent exports. Paths below are relative to `/tmp/scry-overlay-ninth-disproof.1Qrbqj` unless absolute.

## Executable counterexample and finite correction

`TestNinthFreshGenerationVoteEmptyQueryEvidence` establishes an actual synthetic Atlas generation using the unchanged `overlayTestGeneration`/`generationTestCommitBirth` helper, including its real stored episode and canonical generation record. The incoming full original revision declares Atlas(service) with alias Polar Manual twice, then supplies the independently useful Atlas uses Cygnus assertion. The first case has an empty iga family. The second has only an opaque foreign-generation row, which correctly remains outside the relevant query.

Both cases call the genuine `planIdentityEpisode` through unchanged `overlayTestPlan`. Both alias decisions correctly return rejected / insufficient-episode-votes. Repeated current input contributes only one vote. The unrelated primary remains proposed-resolution. Complete raw rows remain equal, events remain zero, complete original slots/revision survive, actual identity/fact writer histories remain empty and no candidate is materialized.

The initial generation-vote path at `internal/memory/store/identity_overlay_controls.go:438` constructs and iterates the exact prefix `iga:<generation ID>:<normalized-alias digest>:`. With no matching rows the loop executes zero times, so no stored vote witness is recorded. The cached path at line 396 merely merges the same empty evidence. `voteTraces` is private and discarded. The report has no generation-attestation query inventory or scope field. Its only explicit query views are the FA-specific `ReferenceViews`. The complete identity inventory's families at `identity_relationship_inventory.go:43` exclude iga, and the fact inventory covers only fa. Thus neither a direct trace, a producer chain, nor the returned complete inventories supplies the missing scoped absence.

The executable assertion enumerates the entire returned plan by JSON serialization and checks for the exact query prefix, after verifying both alias decisions reached the expected vote-dependent branch. Under this exact report schema the query is absent everywhere. This is not a universal test prescribing how every future schema must encode a query: the source/schema audit additionally establishes that no equivalent generation/alias/predicate/count/digest scope is represented separately. Generation and alias inputs can derive the requested query, but do not record the observed empty result or bind it to a complete captured scope. The insufficiency reason itself is not a negative storage witness.

This falls under the existing frozen requirement that alias proposals retain exact baseline controls and positive/negative witnesses and that additional vote evidence be owned before use. It requires no B writer, materialization, support selection, temporal planning, production activation or arbitrary concurrent-writer protection. The smallest correction is descriptive: preserve the actual generation-attestation query scope/result in the same snapshot, including zero rows, and bind each initial/cached consumer to it. Keep exact positive rows and actual EP proof, legacy/generation/current-vote separation, typed reasons and unrelated utility. A fabricated absent point key cannot prove that a prefix query returned no rows. This report does not require one particular representation, a global iga integrity refusal, or permission to consume foreign-generation evidence.

## Independent passing reference audit

`TestNinthFreshReferenceMatrixAndOwnership` supplies five exact strict-decoder-accepted FA fixtures, including a current self-loop, incoming historical reference, current missing source, historical missing destination, and an attribute whose Value resembles an absent slug. It invokes only the actual fixed planner, with a concept-owner alias proposal, missing endpoint primary/hint, a new independent endpoint and an unrelated useful assertion.

The test independently recomputes the full sorted length-framed SHA-256 digest from raw FA keys/bytes. Every returned ReferenceView has the exact version, slug/current-only predicate, fa prefix, total count/digest, ordered matching key/history list and corresponding exact positive raw witnesses. Self-loops count once; current-only excludes history; Value is never an endpoint; missing current/history references defer correctly while unrelated utility survives. Repeated primary/hint query consumers retain their explicit query traces. Mutating returned rows, trace slices and raw witnesses does not corrupt a different trace, the store or a subsequent byte-identical fresh plan.

Static inspection confirms `buildReferenceIndex` and `referenceEvidence` check genuine body phase/owner/poison before cache shortcuts. The index is built from the same genuine transaction and checked against the complete baseline digest/counts. Query row slices are separately constructed; trace clone and vote-trace merging include query dependencies. Disposal removes both reference maps. These new methods were audited statically for invalid-phase cache entry; existing unchanged phase/owner/poison tests exercise the common guards. No new direct invalid-phase harness for these two methods is claimed.

## Whole-unit coverage and limits

Session orientation ran first and was read. I read the complete active objective, ninth through third review inputs, controlling contract with embedded independent corrections, all eight preceding complete disproof reports, delayed-birth design/review, ControllerV3 and identity-mutation reviews, registration and episode-selector contracts and the selector's linked complete design/review. Oversized combined output was followed by separate/ranged reads. Read-only filename guesses for a prior input, fact inventory and neutral policy source names were corrected by locating the actual referenced files; none changed source or test behavior.

All seven overlay implementation files were read completely. I inspected the relevant neutral DTO/declaration/resolve/alias adapters and exact token verifier; original resolver declaration, primary, hint and alias routing; genuine registry, serialized owner/coordinator and fixed finalizers; complete identity/reference inventories and identity/fact mutation and writer ledgers; active legacy and generation recognition and actual EP provenance. The separate complete-admission WIP and rejected assertion bridge were not copied or graded.

Every selected unchanged supplied planner test, eight previous independent disproof suites, ordered-contract/delayed-birth characterizations and policy differential/adaptor tests passes. All 72 previously failing independent leaves remain fixed. Coverage includes complete original slots and observations; nil/present Supersedes and nil/empty inputs; inverse and value-source original-side mapping; actual discovery before proposals and later links; rejected-primary visibility to later primaries and earlier hints; typed failed declarations and independently trusted replacement; exact-natural/canonical-homonym/distinct-name alias precedence; current/history/consumed/retired/unadopted deferral with unrelated utility; old metadata-only usefulness and all old repo refs; no owner transfer; metadata/removal/released-claim producer chains; current-episode deduplication and legacy/generation isolation; actual stored EP including matching-current IDs; owned input/report buffers; phase/owner/poison and genuine discarded-transaction read failure. Relationship regressions retain exact malformed raw suffixes, paired retirement/rejection controls, counterpart natural claims/listings, actual natural endpoints, valid unknown extensions and unrelated opaque-control boundaries.

The exact neutral verifier passes 157 declarations / 248 symbols with only documented DTO/name substitutions and no extra copied declarations. Differential coverage reports 2,323 literals, 16 unary predicates, 18,084 alias projections and 264 declaration projections. The full suite retains the original resolver and 39-relation vocabulary corpora. This is mechanical/adaptor parity, not a new held-out identity/value semantic quality grade or permission to tune dictionaries. Production lexical deduplication remains later work.

No raw/ordinary entity, fact, alias, vote or EP writer is present in the seven planner files. The fixed entry owns full original bytes before waiting, binds its own program to actual serialized registration and supplies no chosen owner, support flag or caller finalizer. Registration precedes candidate proposals; later observations link issued handles. It returns no active facade or handle. Empty writer histories and complete final captured inventory conservation are checked by its own freeze and genuine wrapper. The retained Badger characterization still proves that a stale zero-write Commit may return nil. No dummy write was introduced. This grade concerns this fixed program's absence of writers, same-snapshot/cooperative serialization and final conservation, not arbitrary raw-writer serialization, transient raw-write detection or production all-writer enforcement.

## Commands and retained evidence

All Go commands used `CGO_ENABLED=0` and `TMPDIR=/tmp/scry-overlay-ninth-disproof.1Qrbqj/test-tmp`. Test/check output was retained with tee under pipefail. Durations are test outcomes, not performance measurements.

| Command / log | Result |
| --- | --- |
| `ruby verify-pins.rb copy` → `initial-pins.log` | Both exports: 600 baseline SHA/blob pins, 54 addition SHA/byte pins verified around apply_patch copying |
| Retained planner/eight-prior-disproof/characterization/differential selection → `retained-tests.log` | PASS store 10.862s / resolve 2.490s |
| `go test ./internal/memory/store -run TestNinthFresh -count=1 -v` → `fresh-tests.log` | Compile FAIL in the new test only: guessed `observedSupRef` type does not exist |
| Same command after type-name-only correction → `fresh-corrected-compile.log` | Two empty-generation-query leaves FAIL; full reference matrix PASS; store .536s |
| Relevant store/identitypolicy/resolve `go vet` → `vet.log` | PASS, empty output |
| Exact pinned `verify_policy_port.go` and manifest → `policy-tokens.log` | PASS 157 declarations / 248 symbols |
| `go test ./... -count=1` → `full-suite.log` | FAIL only in those same two new leaves; store 71.387s. All other packages PASS, including resolve 17.352s / daemon 28.135s |
| `ruby verify-pins.rb` → `final-pins.log` | Both complete source exports and 13 exact external input/contract/report/verifier pins PASS |

The initial new test is retained unchanged as `fresh-first-compile-source.go.txt`, along with its complete compile-failure log. The only correction replaced the guessed type name with the actual `observedSupersedes`; no fixture or assertion was changed. The complete final test source is SHA-256 `4988e65493f9047b81de275c1625c3359b52a27485ee477cf4331482bcfebaac`. Once this concrete finite failure was established, I completed bounded required coverage/reporting without additional counterexample expansion.

The export was created with fresh `git archive e097fa6 | tar`. All 54 additions and every authored test/script/report/index use apply_patch; gofmt touched only the new test. Logs were retained through tee. No baseline/supplied/frozen/shared source was edited. No actual stores, backups or replicas, SSH/providers, room/remember, configuration, deployment, cleanup outside synthetic fixtures, or additional agents were used. The root full-suite completion was inspected only as background, never substituted for this grade. Complete artifact hashes are in `EVIDENCE_SHA256.txt`; report hash is returned separately.

NO-GO applies only to this finite private planner. It does not approve FA identity/temporal/hint phases, virtual backdating, assertion content/address/interval preservation, support/materialization/B votes, final ownership closure, lifecycle/all writers, Force/current results, production lexical deduplication, ControllerV3 replacement, adoption/prevention/deployment/live cleanup, recall, binaries/performance/sweeps or any full-goal clause or grading round. Those remain mandatory later obligations.
