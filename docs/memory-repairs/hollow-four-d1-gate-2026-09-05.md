# Independent d1 four-hollow retirement gate — 2026-09-05

**PASS for the exact four-entry candidate on the complete 22:21:40 source and deployed d1f0a958608389a385ac9a9f57ec1eb941015a10 implementation.** No counterexample was found to the four explicit semantic decisions or the exact transaction's preservation, rollback, backup, retrieval, and reintroduction requirements.

This is a bounded replica gate, not an actual-live verdict or whole-goal pass. The lead reports later ingestion and a newer source at 22:31:22. Therefore this report alone does not authorize applying the four to that later store. A fresh semantic/raw closure extension is required. If the separate ChildScribe alias batch is applied first, its exact positive rejection records must be included and proven preserved in that extension. No live mutation, provider call, retry, deployment, or shared-repository edit was performed here. Read-only SSH copied the original sitemap transcript and verified its hash; all other grading writes remained private in this directory.

## Exact inputs and independent method

- Independent `git archive` of `d1f0a958608389a385ac9a9f57ec1eb941015a10` forms this directory's source tree. Production files were not changed; grader tests were added privately.
- Binary used for exact second CLI preview: `/tmp/scry-alias-rejection-deploy-sep05.pPmnn3/scry`, independently hashed SHA-256 `290a14c04ef0cfa9618db3f1a848bc6f3a343ec28d9c197ea49720eedb30b553`. Actual deployment/process identity is separately evidenced by the actual-deploy report; this gate does not claim a new independent deployment audit.
- Source: `/tmp/scry-alias-rejection-deploy-sep05.pPmnn3/mini-child-immediate-222140.badger`, 73,325,931 bytes, independently verified SHA-256 `35a6e1b8f333fa772ea25537bba2c1979addf25d55eced7dd8f2878558f30d45`.
- Exact manifest, copied here as `reviewed-manifest.json`: SHA-256 **`8908910eae87bf9b1f288af86ccc4c2693a0173becb521ee13acd1d21ba9ffd5`**. Its only difference in expectation format from the earlier implementation is the explicit rejection fingerprint. Every current preview exactly matches its complete expectations.
- Independent backup `independent-preapply.badger`: 73,325,923 bytes; SHA-256 `98b10462d5c008295e60b7930e49d04da36a5eb645080de0de6c43eb55e6d512`.

I inspected and reused the prior independent grader's harness, not the builder helper or builder inventory. Added independent checks directly load the immutable source with Badger before candidate Store.Open, traverse every raw key/value without prefix filtering, and compare the entire map with candidate-open and candidate-backup-restored state. All maps are exactly equal, including present empty values versus absent keys. The backup is synchronized and closed by the exclusive maintenance-locked production operation before retirement, restored separately, and compared completely. Reopen checks preserve the exact final map.

The source has 242,380 logical keys. Logical map hashes use sorted JSON serialization of the complete raw string map, separately from physical backup hashes. Exact equality, rather than hash equality alone, is the preservation assertion:

- Source and restored backup: `ff585ff6385640c007abc91299c18160cabaecd61a58de327a02209af8a54978`.
- Independently predicted and actual post-state: `155be48bbb5d49e8016324d6b92abe627834d0684204419cdd3a3764d7802bac`.

## Complete semantic closure

Read all four candidate metadata records, four stored episode records, all 66 same-episode companion facts, and every contextual entity snapshot. Every candidate has zero current or historical fact endpoints, no encoded raw fact-key or adjacency reference, no outside normalized spelling listing, and no outside match under actual hygiene punctuation/plural folding. Each has no listed aliases and exactly its self-owned normalized routing claim. The Git result's repository reference was inspected; it locates the occurrence and does not make the check result a repository identity.

Full semantic comparison against the prior 21:18:21 evidence finds exactly **one difference**: contextual entity `jeff.last_seen` advances from `2026-09-05T15:33:54.809762-04:00` to `2026-09-05T18:11:14.951409-04:00`. All four candidate snapshots, all four source episodes, all 66 companion facts, and every other contextual field are unchanged. `complete-closure-diff.json` contains the entire difference. Companion artifact SHA remains `b1c1048b6821a3c16578e2b38f6812652bf06c396cdee82b9a65801224c1ab66`.

I independently distilled and read the available original source conversations, including the full parent text for the split sitemap episode:

| Candidate | Original evidence and remaining assertion |
|---|---|
| `all-tasks-implemented` | Exact source segment 1839230–2891114 of Claude session a590babc reports all ten tasks done and 432 tests while the final reviewer finds two Criticals and says do not merge. The existing project status attribute preserves the ten-task/432/330-baseline/strict-xfail result; the separate blocker remains. Later same-transcript fix discussion does not make the earlier result an identity or authorize changing its truth interval. |
| `api-2312-tests-passing` | Kimi source from byte 456896 explicitly reports an empty audit router at fcbe101 still passing 2,312 API tests, followed by the new front-door test and 2,313 tests. The stored reviewer documents audit-api fact preserves that exact baseline and gap; audit-api and front-door-audit-test remain. Current distillation ends its conversational source at 491182; the final 94 bytes through stored 491276 are the checked `turn.ended` metadata record. No conversational text is missing. Existing 2,313/2,314 assertions remain untouched rather than reconciled by this operation. |
| `engine-unavailable` | Exact Codex source 838074–879665 contains the original room status/handoff evidence: route-explainer ended engine_unavailable, no handoff was coming, and two dependent tasks were blocked. This is a terminal outcome, not an engine identity. The historical worker status attribute and original invalidation `2026-08-23T22:09:58.897Z` remain intact. |
| `git-diff-check-clean` | Complete original Mini transcript parent 1592552–2392937 explicitly records sitemap commit 00175e8, 14 tests, build success, clean `git diff --check`, and a clean working tree; then the user requires further QA hardening. The stored final-state assertion and separate QA/no-deploy/no-merge constraints remain. The original file was copied read-only and local/remote SHA-256 agree: `2da6a6bd2ba12b55fd4fb007318614133d4c69d8a782a89c614aa429d193e50e`. |

Fresh ingestion scan since 21:18:21 covers 14 added episode records and 199 facts carrying those episode IDs. All fourteen summaries were read. The only newly relevant domain-spelling assertion is the prior four-remnant review report in episode `0f5f38fb...`, independently distilled and read in full from source 2879702–4106295. It describes the previous review; it supplies no new ownership or entity evidence and is not used to justify the semantic decision. Complete new episode/fact evidence is retained in `fresh-ingestion-evidence.json`.

## Exact mutation, defects, failure safety

The whole actual raw map equals the independent prediction. Exactly 16 records change: delete four `en:` records and four `al:` claims; add four `rs:` and four `rt:` tombstones with the exact reviewed IDs and reasons. No replacement fact, adjacency acknowledgement, rehome, or synthetic assertion is involved. Every fact, historical invalidation, provenance field, episode, queue payload, cursor, metadata item, attestation, value-evidence record, other claim, and preexisting retirement marker is byte-identical. There are zero `ar:` records in this source; nonzero rejection preservation remains part of the later extension if ChildScribe is applied first.

| Measure | Before | After |
|---|---:|---:|
| Facts | 80,499 | 80,499 |
| Historical facts | 7,812 | 7,812 |
| Entities | 30,562 | 30,558 |
| Hollow entities | 2,854 | 2,850 |
| Cross-type collision pairs | 484 | 484 |
| Self-loop facts | 1,096 | 1,096 |
| Dangling-endpoint facts | 1,995 | 1,995 |
| Current relations | 39 | 39 |

The complete hollow list is exactly the prior list minus the four candidates. Full loop/dangling lists and relation set are identical. A supplemental grader enumerates all 484 collision pairs rather than relying on a bounded sample and compares them exactly. It also compares the entire 4,433-entry conservative listing/claim-defect inventory, including names/slugs/aliases and claims whose owner is absent. All unchanged; these existing broader defects are not approved or resolved by this pass.

Wrong fourth-group fact fingerprint aborts the complete batch with zero events and no raw change. Wrong fourth-group **rejection** fingerprint separately proves the new d1 preflight fails the entire batch with zero writes. A deliberate failing postcondition observes all four deletions with every fact unchanged, then rolls back the entire transaction and emits no events. Successful backup-coupled apply emits exactly four entity-delete events and no fact event.

Ordinary resurrection using each original entity, the same slug with a different name, or a new slug with the retired name is refused with ErrEntityRetired, both before and after close/reopen. All attempts preserve the raw map. Second previews do not write. Exact deployed CLI dry run returns dry_run=true, applied=0, refused=4 because the four entities are absent. A final whole-map hash after CLI and recall equals the predicted post-state.

Queue truth is independently 0 ready / 0 backoff / 8 parked at 22:21:41 UTC. No queue action was taken.

## Retained retrieval and reproduction

Full local recall with freshly built in-memory snapshot indexes returns each exact retained assertion at rank **1 before and after**: project ten-task result, audit 2,312 baseline, historical unavailable worker result, and clean Git diff result. Historical query time is strictly inside the stored validity interval at `2026-08-20T00:00:00.000000001Z`. Largest response is **12,493 bytes**, below 24 KB. All exact target facts, queries, times, ranks, and full results are in `retained-assertion-recall.json`. These four bounded probes do not replace the broader benchmark floors or fresh fifty-question goal grader.

Passing independent tests: TestHollowFourIndependent, TestHollowD1RawSource, TestHollowFourIndependentGraph, TestHollowCompleteDefects, TestHollowD1RejectionFingerprint, TestHollowFourRetainedAssertionRecall, TestHollowOriginalSources, and TestHollowFourCLINoWrite. Exact CLI dry run also passed. One initial grader build used the wrong AllEpisodes method name, corrected privately. One initial concurrent read attempted to open the same Badger directory while recall held its lock and failed safely; rerunning sequentially passed. Neither was a candidate defect or a live-store operation.

Harnesses and analysis scripts are included here. Main mutation harness creates an exclusive backup path and should be rerun in a fresh archive with path constants adjusted. The immutable independent backup, raw-loaded/restored and retired replica directories, complete before/after fact/entity exports, exact manifest, previews, semantic evidence, original distilled sources, full defect lists, raw delta, and result artifacts make this report reviewable without the builder helper. This report must be extended against the newer exact source before live apply.
