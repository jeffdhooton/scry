# Independent nine-status retirement grade — 2026-09-05

Verdict: **PASS for this exact nine-entry replica candidate only.** The disproof attempt found no identity mistakenly retired, no guessed source, no fact or history loss, no new graph defect, and no unrelated raw-state mutation. This does not approve live application, the broader 502-candidate inventory, the completeness of status cleanup, or final memory-goal completion.

The grader independently extracted deployed commit `393eeec79f80d3b4becff276c4fcffd71fa68ac5` using `git archive` into this directory and wrote independent tests here. No builder helper or builder result was executed or used as proof. The goal objective and status retirement rubric were read. `scry memory orient --cwd .` ran first. There is no repository-root AGENTS.md. No live mutation, deployment, shared-repository edit, remember call, external research, or model call occurred.

## Exact inputs

- Source: `/tmp/scry-migration0160-fresh-sep05.teYtyC/source.badger`, 77,827,733 bytes, SHA-256 `7261cfff0a7957d825855c430dbbc0f333517e806b7c47d8fc03a66fe8fc1548`.
- Candidate: `/tmp/scry-migration0160-fresh-sep05.teYtyC/status-nine/manifest-replica-only.json`, SHA-256 `a739d9d95de7ac8a8ff8e92ea33e3f717cb1cdf2696d09327c64dbc6bd94f36a`.
- Independent backup: `independent-preapply.badger`, 77,827,717 bytes, SHA-256 `e2f01e66588b433e35a467665e7ae02d229de0b5f8e7a0a6cb1ca27ad51f7648`.
- Before raw logical-state SHA-256: `64b1aed04e121c276d6cfd7c6c9ad6fffe552e6eb3d45e7cc9297b264c24e903`. Restoring the independent backup produced precisely the same complete key/value map and hash. These hashes cover a sorted JSON map of raw Badger keys to raw values; they are not physical database-directory hashes.
- Predicted and actual after raw-state SHA-256: `e693ef650a5a3a7b3c32361c1f7d2147e6a3884e7ef2362bd7e4c35752a48c7e`.

## Semantic disproof

Every candidate has exactly one current incoming fact, no outgoing fact, and no invalidated touching fact. All nine are concept nodes without listed aliases. Their descriptions, timestamps, repo references, exact incoming facts, full source episodes, same-episode companion facts, and the source entities' metadata were examined. `semantic-evidence.json` preserves those plus all source-touching companion facts and their entities for review. Repo references here are episode context, not evidence of a separately named artifact.

| Retired slug | Evidence and boundary decision |
|---|---|
| `1-complete-priority-cell` | Episode `a822b883…` and migration-0014 companion explicitly measure one of 24 strict-gate cells, Texas nursing. The licensing project and launch gate remain identities. |
| `164-fields-have-field-evidence` | Episode `37eb697c…` measures 164 evidenced fields. The actual named JSON backlog remains the source, including its file-path alias. |
| `193-pages-blocked-on-authority-verification` | A backlog count, not a separately referable object. Its accuracy is disputed below; the stored assertion is preserved verbatim. |
| `21-suspect-bodies-removed` | Description and incoming fact identify a sanitizer outcome. Episode `98402d39…` and companion facts distinguish the ingestion engine, documentation, and sitemap module, all retained. |
| `50-jurisdictions-published` | Episode `53cff505…` identifies seed/parity coverage and publication activity. The count/result is not a named seed script or release artifact. Publication accuracy is not endorsed; see below. |
| `641-fields-have-content` | A backlog measurement, not the backlog identity or evidence file. Existing numerical conflict is preserved, not corrected. |
| `actively-building-maine` | Episode `b038cfe6…` describes the builder resuming work, with companion paused-checkpoint and 19-gap state. Source remains exactly `dba-filing-guide`; no project deduplication is inferred. |
| `addressing-review-findings` | Episode `5367e979…` explicitly distinguishes implementer activity at seq 45 from the named review thread and rendering task. Those artifacts remain; only the activity state is converted. |
| `ai-sync-tests-15-passing` | Episode `886dd26c…` reports fifteen passing tests after goal-prompt changes. `ai-syncpy`, `test-ai-sync` (the actual pytest file), and the skill remain identities. |

For each replacement, an independent assertion requires the entire original Fact to remain identical except `Dst=""` and `Value=retiredEntity.Name`. This includes source, relation, raw relation, text, validity, timestamps, confidence, and episode list. A separate generic JSON-object check confirms that no unmodeled raw fact field is silently lost.

Factual-review caveats, not identity-retirement failures:

1. The stored `193 pages blocked…` assertion conflicts with episode `37eb697c…`, which says 1,062 blocked pages.
2. The stored `641 fields have content` assertion conflicts with the same episode's 193 content fields. Another backlog companion also says 193 populated and 164 evidenced slots.
3. The `50-jurisdictions-published` name/description must not be cited as independently verified publication coverage: its episode summary says 50 seeded jurisdictions, while a same-episode `dbafilingguide -> status -> 16-states-remaining` fact says sixteen state/DC guides remained intentionally unpublished. Seeding, publication, and temporal scope need separate fact review. The candidate preserves the original assertion without resolving this ambiguity.

These issues do not establish independent identities for the three measured-result nodes. Correcting their numbers or wording inside a structural retirement would violate the fact-preservation rule.

## Ownership, graph state, and transactional checks

All 51,747 alias-index claims and all 30,441 entities were scanned. Each candidate's entire normalized spelling set has one claim owned by itself, no hidden extra routing key, no external entity listing, and no outside match under the actual hygiene punctuation/plural fold. No alias rehome is required. All nine canonical reverse-index entries are present and empty; there are no additional, malformed, or stale candidate adjacencies. Independent previews are ready and their complete entity/fact/alias/adjacency fingerprints exactly match the candidate.

The successful apply changes only these expected records: remove nine entities, nine alias claims, nine old edge facts, and nine reverse indexes; add nine literal attribute facts, nine slug retirement markers, and nine spelling retirement markers. That is 36 deleted keys and 27 added keys, with zero changed unrelated values. Logical keys decrease from 241,340 to 241,331. All episode, queue, attempt, cursor, value-evidence, and metadata records are unchanged.

- Entities: 30,441 → 30,432.
- Facts: 80,203 → 80,203; invalidated facts: 7,792 → 7,792.
- Current relation vocabulary: 39 → 39, identical set.
- Hygiene cross-type collisions: 484 → 484.
- Existing self-loop facts: 1,095 → 1,095; dangling-endpoint facts: 1,995 → 1,995; hollow entities: 2,855 → 2,855. Each complete defect list is identical, not merely its count. These are pre-existing global failures and prevent any claim that the full goal passes.
- The excluded `43-url-sitemap` entity remains byte-for-byte identical, as do its facts and all other noncandidate records. Numeric wording alone was not used to retire it.
- A deliberately wrong ninth-group fingerprint rejects the whole manifest: complete raw state unchanged, zero observer events.
- A forced postcondition error executes after all nine conversions are visible in the transaction, then rolls every conversion back: complete raw state unchanged, zero observer events.
- Backup is nonempty, synced and closed through the deployed backup-coupled operation before apply; a separate restored store equals the full pre-apply state.
- Successful batch emits exactly 27 expected events. All retired exact entity and alias lookups are absent. The second nine-entry preview has no ready/applied items, causes no raw write, and emits no event.

## Reproduction and artifacts

Independent tests:

```text
go test ./internal/memory/store -run '^TestStatusNineIndependent$' -count=1 -v
go test ./internal/memory/resolve -run '^TestStatusNineIndependentGraph$' -count=1 -v
go test ./internal/memory/store -run '^TestStatusNineIndependentRawPayloadAndLookup$' -count=1 -v
```

The first command creates replica directories and an exclusive backup; reproduce it in a fresh directory with the root constant adjusted. Do not rerun it against its existing output. Primary artifacts are `semantic-evidence.json`, `independent-previews.json`, `mechanical-results.json`, `graph-audit.json`, `raw-delta.json`, `raw-payload-and-lookup.json`, the full before/after entity/fact JSON files, and `independent-preapply.badger`. The two independent test files live only in this extracted temporary checkout.

Before any live proposal, take the fresh post-migration source, regenerate exact expectations, and independently recheck scope, facts, claims, adjacencies, and global baselines. The separately pending six-member migration merge changes the source baseline. This report authorizes no live use of these stale snapshot fingerprints and does not grade that other migration.
