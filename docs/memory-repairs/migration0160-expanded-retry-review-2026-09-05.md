# Renewed exact pending-item retry gate

**Conditional bounded PASS for one administrative resumption through unchanged normal `62cf6e0` processing.** This replaces the old insufficient nine-fact-only closure condition. It is not approval of any unknown extracted output, not a collision-repair manifest, and not a determination that `migration0160` is either the SQL file or a clean independent project. That mixed-abstraction finding remains **UNRESOLVED**.

The only eligible ID is `ed50810befba04287fe6676b18db368af6808d32508980ae6e49373009230727`. No retry, provider call, remember, live write or shared-repository edit was performed by this reviewer. Existing fixture tests ran locally; independent backups were restored into review-owned temporary stores.

## Evidence and fresh closure

Exact code was independently archived from `62cf6e0d2d8db9d324da23114df837848e972c8f` into this directory. Fresh source: `/tmp/scry-canonical-62cf-deploy-sep05.G82GKc/memory-20260905T211821Z.badger`, independently SHA256-verified as `3f09b0d6622d4612811b83fc6738f845063c4d19b07ced41c7b893e9b4421e38`, 78,021,343 bytes. Restore was started only after the completed transfer and hash verification. Inventory: **80,308 facts, 30,472 entities, 9,330 episodes, 51,798 alias claims, six pending items, all six parked**.

Independent earlier source: `memory-20260905T210535Z.badger`, SHA256 `ccd3dbd6fc4143458a6682481be5aba422d0ecc29f8f806c1db67c22dc9020b3`. Complete arrays from both sources are retained under `prior/` and `fresh-complete/`.

The complete selected closure consists of 18 entity records, all 36 touching current/history facts, eight full source episodes, every listed name/alias/slug route, all touching endpoint metadata, and 132 same-episode companion facts. The 36 touching facts are all current; none has an invalidation timestamp. Historical companion facts were retained and inspected, not filtered away.

The selected entities are the SQL survivor; mixed `migration0160`; sixth-record concept; semantic gate; metadata guard; actual live apply/report; five/six-record candidates and manifests; older three/five-record report and closure artifacts; independent replica verification; live-apply approval; the earlier `migration-0160-duplicate-group`; and the distinct task-events correction-exposure decision. [Complete records and hashes](fresh-complete/closure.json) and [companion facts](fresh-complete/episode-companion-facts.json) are the evidence, not an inferred merge membership list.

I independently confirmed all 18 entity records, all 36 touching facts, all eight source episodes and all selected routes are unchanged from 21:05. No outside entity lists one of these normalized keys or the checked punctuation-folded spellings. The earlier exact deployed-hygiene audit additionally enumerated every broader singularized fold and found only the already reported mixed migration pair. Three older report/artifact records have pre-existing absent **slug-only** routes (`five-record-regrade-reviewmd`, `full-five-semantic-closurejson`, `three-record-verification-reviewmd`); their actual names route normally. This is a recorded legacy condition, not permission to fill or rehome those keys during this retry.

The fresh global addition is exactly episode `8f86c6cdf576a564060bbd71cbbed4fec38cd3dd6bf04e9093d81d117ccc6d8c`: the complete manual six-status-node cleanup operation note, ingested 21:16:29.138188 UTC. It adds nine facts, five entities and eight previously absent alias keys; `scry` and `scry-memory-quality-goal` receive LastSeen-only refreshes. No old fact or episode is removed/changed and all six pending records are identical. The changed Scry endpoint metadata is explicitly captured in this fresh baseline. The note states that no network authorization was granted; this review grants none either.

## Exact note and semantic decision

The complete pending payload is in `closure.json`, not the truncated queue-list response. It remains manual/manual, parked, attempts=1, Force=false, empty CWD, CwdIsRepo=false, and uncompleted. Its occurrence, enqueue and next-attempt time represent 20:09:19.093118 UTC. Hints remain exactly `scry` and `db/migrations/0160_task_evidence_rules.sql`; original alias-conflict error is unchanged.

The note records Scry's **already completed** six-record merge at 20:07 UTC, exact manifest SHA `547b324b4a0c83bcec6a345b46a8e2f3e80be5b2178b002d125d2aaf8f29e2c3`, rollback backup `memory-20260905T200709Z.badger`, receipt path, preservation of 80,203 facts/nine file facts, fixed-suite results below two floors, and the unfinished goal. It neither requests another repair nor names a replacement owner for `migration0160`.

The SQL file, memory records representing it, repair candidates/manifests, actual apply, and review reports are different abstractions. The new project's three assertions still mix these abstractions. In particular, preserving the entire Scry graph is a replay/apply observation, not behavior of the Docket SQL file. The sixth-record concept describes a memory record; its original source describes the represented file. Nothing in this retry note settles that ambiguity. The earlier duplicate-group concept describes a reviewed candidate group, not automatically the physical SQL file. Candidate PASS/FAIL and live-approval statements remain scoped to their original snapshots and times.

Resuming a durable operation note through normal ingestion is not an explicit historical merge or ownership decision. No concrete new alias-transfer or cross-identity merge path was proven by these inputs. The ambiguity therefore does not, by itself, require choosing an owner or asking the user to choose one before this one administrative resumption. It does require preserving and independently examining any output touching these records.

## Safeguards checked and their limits

Read exact `cmd/scry/memory.go`, `internal/daemon/memory_queue.go`, `internal/memory/store/pending.go`, `internal/memory/queue/queue.go`, `internal/memory/resolve/resolve.go` and existing alias/name guards. The one-ID handler only resets Parked, Attempts and NextAttempt for the matching eligible item. Omitting the ID is broad retry and excluded. Text/source/time/hints/CWD/Force are not rewritten. Normal manual completion stores the **entire original note** as the episode summary. Apply is atomic and records completion last; Force=false makes an already completed episode a no-op. Successful Apply precedes pending deletion. Alias-claim, invalid-slug, retired-entity and fact-key deterministic conflicts repark the payload intact rather than loop or partially commit.

The qualified SQL name still has exactly one normalized canonical owner, `0160-task-evidence-rulessql`; the natural-slug entity `dbmigrations0160-task-evidence-rulessql` is absent. All eight approved SQL keys route to the retained owner. `migration0160` remains a different normalized key, not a second qualified canonical owner. The canonical-name exception does not authorize joining it to the SQL file; alias preflights do not transfer existing claims.

Reran and passed existing canonical-name routing/ambiguity/generic-reference tests, deterministic queue parking tests, idempotency, fallback collision whole-episode rollback, and the one-ID queue listing/retry test, across resolve/queue/daemon packages. These are fixture tests, not invented provider output for this note. No extraction was requested or replayed.

**Normal ingestion is not a frozen-state transaction.** It may add facts/entities, append provenance to a matching assertion, raise confidence, move ValidFrom earlier for a supported restatement, apply temporal invalidations, refresh LastSeen, admit unclaimed aliases, or promote a concept stub. These paths do not all produce deterministic conflicts. An inaccurate semantic assertion could therefore be accepted without parking. This gate does not claim otherwise or pre-approve such a change to an unresolved identity. Post-attempt inspection is required; an unchanged fact count or successful queue drain is not semantic approval. One retry command may involve configured provider fallback/transport backoff, not exactly one provider call.

## Mandatory immediate preconditions

The lead must take and verify a fresh nonempty rollback backup immediately before the one-ID invocation, confirm exact deployed code remains active, and recheck the full pending record plus complete fresh closure. Hashes below use SHA256 over Go `encoding/json.Marshal` of the corresponding ordered arrays/maps/struct; `cmd/independent-closure/main.go` defines the derivation.

| Required input | SHA256 |
|---|---|
| Exact pending struct | `c51e3cc9a64cd7978db4c8e19711098b9114d93f075b03829608d2090fe8da4e` |
| Exact text bytes | `f2dcf3cb971179b59c4833e89c9d5b4e14c3b8a670eaf899018c00d33aa1d31f` |
| 18 closure entity records | `3a1e004a1ece960061e4202d3a60cd4d3fc7c6a37ce4e2b24fa9d1ba7c6a60d9` |
| All 36 touching facts | `06d7e9d6b249f0a53dbe4ea81f44214d3be7f0d11144dec93058d198fa555a94` |
| Eight complete source episodes | `cb8167e86e21d04f7f0706b5c4d8ac797677a390ceaf958e09af122c1dde18eb` |
| Complete selected normalized routes | `2750eb7cfd062c31f68680e8520e4cb9ca92f681b8431834aff1afb8da50c39f` |
| Outside listing set (null) | `74234e98afe7498fb5daf1f36ac2d78acc339464f950703b8c019892f982b90b` |
| Complete entities/facts/episodes/claims/pending baseline | `e8d11bc0e85eba8a74f7a8285b3f09d14d03b2c97e757a232702cc954c6c6c00` |

Also repeat the whole-inventory semantic-neighborhood search, unique qualified canonical-owner/absent natural-owner checks, full aliases/folds and touching facts, not just hashes of the old selected set. Any new related entity/fact/episode, changed protected input or other unexplained global drift invalidates this gate until reviewed. Recheck no completed marker, all other five parked payloads and queue eligibility. No scheduling-reset API compare-and-swap freezes these fingerprints; immediate single-lead sequencing with no concurrent repair is a requirement, not an atomicity claim.

## Mandatory result gate

After one invocation, inspect this exact item until completion or repark and capture a post backup before any further repair or remember. Leave any new deterministic conflict parked intact; do not invoke retry again, force, edit the note, alter timestamps/keys or change aliases to make it fit.

On completion require the original full manual note/source/provenance, no change to the other five parked items, and an independent full pre/post graph delta. Preserve every pre-existing fact's text, raw relation, endpoints/value and historical evidence; account explicitly for any normal provenance/confidence/time merge rather than treating every changed byte as either harmless or automatically forbidden. Do not approve unsupported temporal invalidation or semantic changes merely because the pipeline accepted them.

The 18 existing identities must remain distinct with their existing names, types, descriptions, aliases, repository associations and owner claims protected against unreviewed repair. A mere LastSeen refresh or explicit source-backed observation is not an identity transfer, but any other metadata change, new alias, new endpoint assertion or fact merge involving the mixed project/sixth-record/group must receive explicit post-attempt semantic review. No result may be accepted as proving that project owns the SQL file or that the SQL file performs the repair. Check original nine file facts, all old keys, no recreated natural-slug file husk, all changed keys, history/dangling/hollow/collision deltas and every new/modified episode fact. Unexpected changes mean stop for review with both backups retained—not a guessed compensating edit or an automatic rollback over unrelated ingestion.

This renewed gate authorizes no graph-wide cleanup and certifies neither unknown provider semantics nor the full memory-quality goal. It permits the single bounded normal retry under the immediate preconditions and subsequent result gate above.
