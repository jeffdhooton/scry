# Independent fresh four-hollow-status live-candidate gate — 2026-09-05

**PASS: the exact four-hollow-remnant candidate is ready for the reviewed live retirement workflow on deployed `62cf6e0d2d8db9d324da23114df837848e972c8f`.** All four are nonidentity status/result remnants with no current or historical graph edges, no hidden raw edge/index reference, and no outside spelling owner. Every existing fact stays completely unchanged; no replacement or synthetic assertion is added.

This fresh gate extends the independently checked earlier replica to the complete 21:18:21 source. It is not an actual-live apply verdict, broad cleanup-completeness claim, permission to retry parked work, or full memory-goal pass. The lead must use the exact reviewed manifest, the deployed maintenance-locked nonempty-backup/expectation-recheck operation, and independent actual-live postconditions. New affected-input or semantic drift reopens the gate.

All review writes stayed inside temporary independent archives and disposable replicas. No live mutation, retry, provider/network call, remember call, shared-repository edit, or deployment occurred. Recall indexes were built locally in memory from the snapshot; no hosted model or embedding service was invoked.

## Independent source and artifact verification

The grader independently archived exact commit `62cf6e0d2d8db9d324da23114df837848e972c8f`, restored the original post-six source into `/tmp/scry-hollow-four-grade.FDIH38`, and then independently archived/restored the new complete source into this directory. Neither builder helper nor builder inventory, including the failed partial-download scratch inventory, was used as proof.

- Fresh source: `/tmp/scry-canonical-62cf-deploy-sep05.G82GKc/memory-20260905T211821Z.badger`, **78,021,343 bytes**, independently verified SHA-256 `3f09b0d6622d4612811b83fc6738f845063c4d19b07ced41c7b893e9b4421e38`.
- Manifest: `/tmp/scry-canonical-62cf-deploy-sep05.G82GKc/hollow-four-fresh-replica/manifest-replica-only.json`, SHA-256 **`f1b67ae517b3ec4e394879c28df28ef23c0978e9390a9bf6e70016967eb28e5d`**.
- Independent backup: `independent-preapply.badger`, **78,021,351 bytes**, SHA-256 `ae41bc5e5dd3a7763778f56c4f9c60a4e7fcd23b65ddb7ca3d9be84008db0c13`.
- Complete source / independently restored backup raw logical hash: `2661f53e2e12546d153de5b39b9b922edb067fb0348028a72a4a9466c5588f8b`.
- Independently predicted / actual replica post-state raw logical hash: `b4b45ebcf514a66d194fd446a7589b6eda8469e86214ad671afd1514cb1146ab`.

Logical hashes encode the complete raw Badger key/value map with sorted JSON keys; physical backup-file hashes are separately measured. The independent backup was synced/closed before apply and restored into a separate store whose entire raw map matches the source.

## Queue truth and full semantic closure

On the independently restored fresh source, deployed `Store.PendingCounts` returns **0 ready / 0 backoff / 6 parked** at `2026-09-05T21:18:22Z`. The new source is independently checked, not presumed quiet from the preceding live observation.

The entire fresh semantic evidence is **byte-identical** to the evidence independently read on the earlier source: full four entity snapshots, all four full supporting episode records, all 66 same-episode companion facts, all their contextual entity metadata, and complete candidate alias claims. This was an explicit full-structure comparison, not acceptance of matching manifest fingerprints alone.

- `semantic-evidence.json`: SHA-256 `8ae6c7f06adb13fbaffdae3c7e1fc0d29a1b908815b15281d8c745663b8338d8`.
- `all-companion-facts.json`: SHA-256 `b1c1048b6821a3c16578e2b38f6812652bf06c396cdee82b9a65801224c1ab66`.
- `previews.json`: SHA-256 `13bd4dd315c5231db48e19fdc57eee94bc5f2605a718655e4eb63ec03a20c5b1`.
- `fresh-source-check.json` records the direct queue check and complete semantic-evidence equality.

The nonidentity decisions and surviving assertions are:

| Remnant | Independent evidence and preserved assertion |
|---|---|
| `all-tasks-implemented` | Its metadata calls it an end-of-episode state. Episode `14e2e6f3…` reports ten implemented tasks and 432 passing tests, while the final review blocks merging. The existing current `hermes-agent-templates -> status -> all-tasks-implemented` attribute preserves that complete result, including its baseline and xfail detail. The independent `final-review-do-not-merge` blocker, Critical findings, and campaign identity remain intact. |
| `api-2312-tests-passing` | A measured pre-fix test-count result, not the audit API, test file, or named suite. Episode `5eb04f98…`, the real `audit-api` and `front-door-audit-test` entities, and the unchanged `claude-reviewer -> documents -> audit-api` fact preserve that an empty audit router still passed 2,312 tests at fcbe101. |
| `engine-unavailable` | A terminal worker outcome, not the engine/worker identity or a named policy. Episode `831a9ca1…` and the existing **historical** `codex-repair-route-explainer -> status -> engine-unavailable` attribute preserve the no-handoff assertion. Its original invalidation timestamp remains intact; it is not promoted back to current. Dependent-task blockers and the worker identity remain. |
| `git-diff-check-clean` | A command/check result after the sitemap commit, not Git, the worktree, or the sitemap module. Episode `98402d39…` and the unchanged full `slookup-ingestion -> related_to -> tests-passing` final-state fact explicitly preserve `git diff --check` passing with a clean tree. Outstanding QA-hardening findings and no-deploy/no-merge constraints remain. |

Each candidate's `CreatedAt`/`LastSeen` matches its supplied supporting episode's occurrence time, and the descriptions correspond to that episode's outcome. No entity owner is inferred from spelling; there are no fact moves to assign. Candidate metadata, including the ingestion repo reference on the Git result, is retained in the verified backup and review artifact.

The existing 2,313/2,314 reports are untouched. In particular, the `vitest -> related_to -> api-gate-passing` fact says 2,314 after the fix while another front-door-test assertion says 2,313; this removal does not reconcile them, invent finer chronology, or correct their arithmetic. The original 2,312 reviewer assertion remains independently recoverable.

## Raw absence, ownership, and exact mutation

The scan includes all **80,308 current/historical facts**, all **30,472 entities**, and all **51,798 alias-index claims**. It checks fact payload endpoints as well as encoded raw `fa:` source/destination slots and raw `adj:` endpoint slots. None references any of these four entities. No malformed, stale, nonempty, or hidden candidate reverse index is present.

All four have no listed aliases and one normalized claim owned by themselves. A full entity/claim scan finds no hidden extra routing key, outside normalized listing, or outside match under the actual hygiene punctuation/plural fold. Independent previews are ready and match every complete entity/fact/alias/adjacency expectation. There are no replacements, adjacency acknowledgments, or rehomes in this candidate.

The exact post-state differs only in **16 records**: delete four `en:` records and four `al:` claims; add four `rs:` slug tombstones and four `rt:` spelling tombstones with exact reviewed IDs/reasons. The complete actual replica map equals this independent prediction. All fact records, historical invalidations, provenance, source metadata, unrelated claims, episodes, blockers, queue entries, cursors, attempts, and prior repair markers remain byte-for-byte unchanged. No fresh synthetic fact is created.

| Measurement | Fresh source | Replica after removal |
|---|---:|---:|
| Entities | 30,472 | 30,468 |
| Hollow entities | 2,855 | 2,851 |
| Facts | 80,308 | 80,308 |
| Historical facts | 7,800 | 7,800 |
| Cross-type collisions | 484 | 484 |
| Self-loop facts | 1,095 | 1,095 |
| Dangling-endpoint facts | 1,995 | 1,995 |
| Current relations | 39 | 39 |
| Logical raw keys | 241,717 | 241,717 |

The full hollow list is exactly the old list minus these four; all other complete defect lists and the relation set are identical. Existing global defects remain broader-goal failures.

## Failure safety, reintroduction guard, and repeat preview

A wrong fourth-group fingerprint aborts the complete batch with identical raw state and zero events. A deliberate postcondition error observes all four deletions with the complete original fact list unchanged, then rolls back every mutation with zero events. The successful backup-coupled apply emits exactly four entity-delete events and no fact event.

Ordinary writes attempting each original entity, the same retired slug with a new name, or a new slug with the retired name are rejected with `ErrEntityRetired`. All twelve attempts cause no raw change. The checks are repeated after closing and reopening the store, with the same result. Exact entity/name lookups remain absent, and durable retired-spelling checks remain true. This proves the tested ordinary store-write paths cannot recreate these remnants; it is not a new global resolver-corpus claim.

Second direct previews are nonready/nonapplied and change no state/events. A separate exact CLI dry run from the archived deployed code returns `dry_run=true`, `applied=0`, `refused=4`, all because the retired entities are absent. A subsequent complete raw hash check verifies that both the CLI and the recall probes below made no store write. Evidence: `exact-cli-second-preview.json` and `cli-recall-no-write.json`.

## Remaining assertion recall

Four independently chosen bounded queries were run before and after removal through the repository's full local recall path with an in-memory index learned from the restored snapshot. Every retained target is the exact same complete fact before/after and ranks **1** in both phases:

| Target | Query | Mode |
|---|---|---|
| Ten tasks / 432 tests | `hermes-agent-templates all tasks implemented 432` | Current |
| 2,312-test empty-router gap | `audit-api 2312 empty router fcbe101` | Current |
| Engine unavailable / no handoff | `codex-repair-route-explainer engine_unavailable no handoff` | Historical as-of |
| Clean diff check | `slookup-ingestion git diff check clean sitemap` | Current |

The engine probe uses `2026-08-20T00:00:00.000000001Z`, chosen strictly within the fact's stored validity interval. This tests historical recoverability, not current truth. Its invalidation remains `2026-08-23T22:09:58.897Z`.

The largest response across all eight before/after results is **12,492 bytes**, below 24 KB. Full results, ranks, exact retained facts, and timestamps are in `retained-assertion-recall.json`. These four probes establish bounded remaining-assertion retrieval; they do not replace the independent broad recall suites or held-out grading requirement.

## Artifacts and handoff

All artifacts are in this directory: complete entity/fact exports before and after, full semantic evidence and 66 companions, previews, mechanical results, graph audit, fresh queue/closure check, exact raw delta, independent backup and restoration, recall results, CLI/no-write proof, and grader-written tests.

Passing independent checks: `TestHollowFourIndependent`, `TestHollowFourFreshSource`, `TestHollowFourIndependentGraph`, `TestHollowFourRetainedAssertionRecall`, and `TestHollowFourCLINoWrite`, plus the separately executed exact CLI dry run. The main test creates exclusive output paths and must be reproduced in a fresh directory.

The lead may now use the exact reviewed live workflow for these four on this deployed implementation, preserving its nonempty actual backup and locked revalidation. Changes to candidate/source episodes, companion context, claims/listings, or graph references require review before proceeding. Actual live deletion, later queue processing, subsequent real sweeps, broader status inventory, and full goal completion remain separately graded work.
