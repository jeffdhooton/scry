# Independent refreshed semantic and complete-replica gate: Child `stops table`

**Bounded PASS for the exact supplied 22:53:12 snapshot and one-drop manifest. Live freshness is unresolved.** Root reports later ingestion, so this is not an immediate-live apply gate. It does not certify the separately running actual four-retirement grade, any new snapshot, or whole-goal completion.

Only remove Child's literal `stops table` alias and its claim, recording the exact owner-specific negative decision. No positive owner is established. Preserve all facts, current/history provenance, other metadata, and all earlier decisions. Old-binary-only rollback remains unsafe because older writers ignore rejection records; the fourth rejection does not relax that limitation.

## Independent acquisition and input pins

I read the complete proposed review, proposed source closure, and independent semantic review in `docs/memory-repairs/child-stops-table-*2026-09-05.*`. I created a fresh private `git archive d1f0a958608389a385ac9a9f57ec1eb941015a10` at this directory and authored `cmd/stops-grade/main.go`, `audit.cjs`, and private additional-path tests. The helper-bearing tree is not described as pristine. No shared-repository edit, live write, provider request, queue retry, installation, or deployment occurred. The user's untracked workflow assessment was untouched.

Independently checked SHA-256 inputs:

| Input under `/tmp/scry-alias-rejection-deploy-sep05.pPmnn3` | SHA-256 |
|---|---|
| `memory-20260905T225312Z.badger` | `efa7466e022fb6a67918876aba8960eff9ae4aa1b1027eeeac52dd41ead9058a` |
| `mini-child-final-223122.badger` | `b669593b978041a646b8c6f3f3dc2cee5eb8cbad0e83d48063915a7a7aaeaea0` |
| `stops-table-post-four-225312/manifest-replica-only.json` | `94484a6d7929e98ac42b06aa49b9276b0fc812976bc4cf275613abcf3ffd5c87` |
| pinned `scry` binary used for ten offline suite runs | `290a14c04ef0cfa9618db3f1a848bc6f3a343ec28d9c197ea49720eedb30b553` |

The complete supplied post-four source contains 80,602 facts, 30,607 entities, 9,354 episodes, 55,616 adjacency keys, 51,958 claims, three rejection keys, 10,853 attestations, 3,124 cursors, ten pending rows, five metadata keys, 19 retired-slug and 19 retirement records, and 628 value-evidence records. Transfer provenance and the live origin of these supplied files are root's evidence; my proof is the supplied complete file and its independently restored content.

## Exact technical gate

Both complete sources were loaded directly through Badger `Load` before candidate `Store.Open`. Every key/value was enumerated without family filtering. Candidate Open preserves the supplied raw map, and production Restore into another new directory yields exactly the same map.

I independently reconstructed every Expected field from directly loaded entities, facts, actual claims, presence, all normalized outside listings, and all prior owner-specific rejection records. This is not agreement between two calls to production Preview. Exact results match the manifest:

- Plan: `ef7a0970016251e74b41e2db69c5d5c0d7b4b03c49e7a24a0b640df66906d35e`.
- Child entity: `c079c7623ded84da512dae9c098b30fda736f12c51589ba9057fadb0fe1b95f5`.
- All 2,106 touching typed facts: `5d879da1900f8e146b5e98858efa44e77445c0ad21b5b21b20b130badb1913fa`.
- Prior three-rejection subset: `1a40bf4d7af133a80d2109949d4f21a45194ec5871a74630aeffac238dfe392a`.
- `stops-table` is present, claimed by Child, with exactly Child in the complete listing set.

The candidate's atomic backup/apply returned Applied with a nonempty 73,553,050-byte private backup. Directly restoring that backup reproduces every original key/value. Its SHA is `a435d03c204c8aea46961c334ac89cae9ed51ac2f279355935c9a3e86baabdea`.

An independently constructed predicted whole map equals the actual private post map. The only three differences are `en:childscribe-laravel` (40 to 39 aliases, preserve exact remaining order and every other field), deletion of `al:stops-table`, and addition of `ar:childscribe-laravel:stops-table` containing exactly the literal alias, entity, manifest why, and plan. Every other key/value survives, including the prior three rejections and all 19/19 retirement keys. Every fact, history field, endpoint, episode array, queue payload, cursor, attestation, and metadata value survives byte for byte.

Complete compact-JSON maps from raw keys to base64 values hash to before `73a7e2638fb78aa9fbcd56b66d4cef5e2bc0028af47e4608fb8633a865ad96b4`, and predicted/after `d5282cdcef2568327a42934eae9644a377a8d9f2e012c452f358e338d691f32e`. These are JSON-map hashes, not length-framed raw-stream hashes. Full maps and exact key deltas are retained.

Second preview is not ready. A second private backed apply refuses; all database bytes remain unchanged. Post backup/direct restore/reopen preserves every rejection and every other key. Stale full Child entity writes, direct claims, and atomic writes with an earlier sentinel mutation all refuse and roll back completely. Rehome to rejected Child refuses. Both real Child/stops merge directions, with fresh Expected and proposed metadata, refuse specifically on rejection inheritance and leave the whole map unchanged.

Two real normal resolver Apply calls using distinct synthetic episode IDs and `stops table` / `STOPS_TABLE` create their episodes while leaving the rejected alias unlisted/unclaimed and all 80,602 facts and four rejection records intact. A subsequent private synthetic distinct identity remains representable. That fixture establishes technical representability only; it assigns no live or semantic rightful owner.

Relevant deployed-commit fixture selections also passed for missing/wrong Expected, entity/fact/claim/outside-listing/plan/rejection drift, backup write/sync/close failures, atomic rollback, concurrent writer exclusion, rejection persistence, rehome, and merge safety. Additional exact-snapshot paths passed `TestStopsExactExtraPaths` (4.783-second package). Final complete raw rereads after all offline benchmark reads passed `TestStopsFinalReadNoWrite` (1.949-second package).

## Refreshed semantic closure

The independently restored 22:31 raw map hash is `192ae88274b5686bae273435f9954414e3a3b2f435516108e3a2ece7e9f42aa6`, exactly the earlier independent semantic source. I rehashed all twelve original SourceRef byte spans and all twelve retained complete projected texts. Every length/hash agrees with the prior independent closures, and every corresponding current stored episode object equals its 22:31 object. This is explicit reuse of the prior independent source adjudication after full closure equality, not a claim that this turn manually reread all twelve projections or omitted historical tool/thinking payloads.

The complete fresh comparison preserves all five separator-variant matching fact rows, all 58 matching-episode companions, all 164 expanded twelve-source companion rows, and all 2,106 Child current/history rows. Their compact raw-object array hashes remain respectively `d5c0906dad0f2e55a77789799f4e820d71d41142aa7bac943de554153afc0799`, `526643b8f4b34b69b17253b43610d84bf72d0e14d36274d91391f1cba7928be5`, `d76ea10656fb14757c0166aa179716611b818e72a6e9255cb064d48955bcdc59`, and `fdcf195d2de089b776504b11a2a608763c009740fa0fb0a88e1cb14441b533de`. The raw-object hash differs from typed Expected fact serialization; both are reproduced and labeled.

Whole-source drift from 22:31 is 77 added, eight changed, and 11 removed raw keys. Complete object delta is 18 new/changed facts versus two old versions, ten new/changed entities versus seven old versions, and one new manual episode. I read every changed old/new object and the full new episode. New facts describe the CADFormats private release; two prior status assertions are preserved with invalidation timestamps. None introduces a Child table identity or new matching table counterclaim. The other changes comprise the prior three Child alias removals and four retired entities; I do not infer actual-four grade approval from their presence.

All 174 broader stop/weight-ticket/0043/0112 candidate entities remain identical except Child's earlier three selected alias removals. Only Child changes within the expanded companion endpoint closure; no outside entity acquires the exact normalized spelling. Thus neither new entities, fresh fact prose, original sources, episode metadata, nor changed alias context adds an ownership blocker for this exact one-drop disposition.

The substantive negative remains the concrete Docket weight-ticket FK table source and its distinction from the `0112_operations_routes_stops.sql` migration and `stops/` directory. The three recurring-plan substring matches remain distinct table-contract discussions. Preserved contaminated Child companions remain UNRESOLVED for separate endpoint review. Earlier row-copy claims, the final no-row-copy guard report, out-of-owned-set test disclosure, and later migration metadata remain historical counterclaims. They are not silently corrected or endorsed. Child's contaminated description, repository references, remaining aliases and all other facts are outside approval.

## Complete structural and retrieval controls

Uncapped full pre/post structural inventories are identical: 2,441 dangling endpoint occurrences, zero missing source episodes or missing/extra adjacency, 3,867 missing listing claims, 505 listing-owner mismatches, zero dangling claim owners, 28 unlisted claims, 462 normalized multi-owner spellings, 27 exact-normalized multi-type spellings, 2,850 entities without any fact, 2,968 without current facts, and 1,096 self-loops. Complete lists are retained, not just counts. No outside listing is stranded. These definitions are exact-normalized structural inventories, not production's broader folded collision heuristic or semantic approval to retire zero-fact entities.

Ten pinned offline benchmark runs preserve hits, missed-question sets and mean answer rank. All runs are under the payload cap:

| Suite | Before → after hits | Maximum bytes before → after |
|---|---|---|
| heldout-2026-09-03 | 51/62 → 51/62 | 12,110 → 12,110 |
| heldout-b | 29/66 → 29/66 | 13,370 → 13,371 |
| probes | 7/7 → 7/7 | 10,097 → 10,099 |
| tuning-strict | 45/50 → 45/50 | 11,530 → 11,531 |
| tuning | 47/50 → 47/50 | 11,530 → 11,531 |

Payload and latency outputs are not claimed byte-identical. The reporting helper initially assumed the zero-miss probes result contained a `misses` array; it was corrected to handle the omitted empty field and then passed without rerunning or altering any suite output. The 53/62 and 34/66 acceptance floors remain unmet; no whole-goal PASS is granted.

## Evidence and limit

Evidence root: `/tmp/stops-postfour-independent.Nq4iJP/evidence`. `file-hashes.json` pins the technical result, independent Expected, exact deltas, source-span verification, semantic equality/drift, complete defects, merge refusals and full benchmark controls. Selected file hashes: technical result `d3dda261c0940b76a5769d924ea463e291d1399c26d6d86db771d9cdd75bc09d`; source checks `23de70618779d5ab15a40ad2380108716409d146a735e51e84fcb8b497b887c3`; semantic equality `aabb2b42014de071b2bf3b9fc6706ade46ff6a487d8c82d9dfbf53e40629c3d8`; semantic drift `c6b5de6a169e62079b5dcbe403d08573289c1bf349030444f2427e8192bbc062`; identical before/after defect files `d8b6d9ffa6deddf685ebdefb8979f9d291ba35e5f8299456b609b79e157ca2ec`.

Live freshness remains BLOCKED pending the independently reviewed extension root says it will supply after later ingestion drains, followed by exact immediate-live input checks. This bounded snapshot PASS grants no broad 49/33 backfill, other alias or fact correction, rehome, old 41-group operation, queue retry, or timestamp nudge. A future actual live apply still needs its own complete atomic pre/post audit and controls.
