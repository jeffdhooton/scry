# PASS — second ten Hermes-trio dispositions only

Independent disproof found no required change to the four proposed `KEEP_ENDPOINTS` and six explicit `UNRESOLVED` decisions. This approves the bounded evidence ledger, not a live repair, a relation rewrite, metadata/alias cleanup, current deployment health, the remaining 849 snapshot records, or the complete goal. No retry is authorized or executed by this review.

## Pinned evidence and verification

- Exact source: `/tmp/scry-canonical-62cf-deploy-sep05.G82GKc/memory-20260905T210535Z.badger`, independently verified SHA-256 `ccd3dbd6fc4143458a6682481be5aba422d0ecc29f8f806c1db67c22dc9020b3`.
- Submitted ledger: `docs/memory-repairs/hermes-second-ten-review-2026-09-05.json`, SHA-256 `782e61fef4adfa1e15b92d49806e713c22668bb183adf0b27d027dc46e2ffbd5`.
- Full rubric: `docs/memory-repairs/hermes-ownership-rubric-2026-09-05.md`, SHA-256 `6b5fa340896aad79629c91940d69e490fb6c091a43ec474258c45ef9c2b9939c`.
- Independently archived exact code `62cf6e0d2d8db9d324da23114df837848e972c8f` into this directory's `code/`; custom read-only inspection helper is `code/cmd/independent-hermes-ten/main.go`. No builder helper or existing replica was reused.
- Two fresh restores, `replica/` and `replica-key-recheck/`, independently contain 80,299 facts, 30,467 entities, 9,329 episodes, 51,790 alias claims, six pending items, and 859 unique current/historical facts touching the trio. All ten complete selected payloads and hashes equal the submitted ledger. The second run additionally opens its replica through Badger read-only and gets every exact supplied raw `fa:` key, verifying its complete fact fingerprint.
- Read all ten full stored source episode objects, all 141 same-episode fact objects (this count includes the ten selected records, not 141 additional records), and all eleven distinct selected endpoint entity objects. Stored episodes are summaries/provenance records, not recovered transcripts. Companion records with extra provenance episodes were read in full; this is not a claim to read every other episode referenced by those out-of-scope companions.
- Read all 342 lines of `/Users/jeff/workspace/hermes/hermes-ops/leadgen/outreach/audit.py`, independently verified SHA-256 `616724008ff8696cbd3823cbde86427efc4b9b2c125da4b00e3e4dbaafe53365`. Its last file commit is `6709519bd4ca0ed238947f989d7bffa4d257ed20`, August 6, 2026. It corroborates renderer/repository identity, not the precise July deployment or any authority for outreach, crawling or recrawling. Nothing in that project was executed.

The inspection helper asserts equality before/after of all fact, entity, episode, alias-claim and pending payloads. Both restores yield the same unchanged aggregate SHA-256 `92af21ed87b4b68e69bc60a205b829aa5bea56299349f225383f0d352255d3c0`. Exact `hermes-ops`, `hermes`, and `mac-mini` resolve to three distinct, existing identities. This is not a complete raw-key/schema audit or an alias-cleanliness endorsement.

## Per-record findings

| Input row | Verdict | Independent finding |
| --- | --- | --- |
| 1. Alias admission gate `uses hermes-ops`, raw `tested_with` | PASS KEEP_ENDPOINTS | Source is the resolver gate; repository/Mini case is its test input. Full B3 episode, metadata, and implementation companion agree. Instrumental use of a test subject is supported; the repository does not become the runtime or host. Preserve raw relation and scenario sentence. |
| 2. Audit renderer `part_of hermes-ops` | PASS KEEP_ENDPOINTS | Tool metadata names `leadgen/outreach/audit.py`; the full pagination episode and WeasyPrint/table companions identify the repository artifact. The later local file contains the rendering entry points and fixed-layout summary table. Cwd alone was not relied upon. |
| 3. avoid-ai-writing `deployed_on mac-mini` | PASS KEEP_ENDPOINTS | Installation source episode explicitly identifies Jeff's Mini. Gateway enables-skill and channel-path companions independently distinguish runtime integration from host installation. This is historical ownership only, with the original coarse date preserved. |
| 4. avoid-ai-writing `part_of hermes`, raw `candidate_for` | PASS UNRESOLVED | Evaluation and proposed runtime do not establish membership. The evaluation episode occurs at 14:58, installation episode at 15:26 on August 28. The installation fact has a coarse midnight timestamp, so record ordering must not be inferred from its `valid_from` alone. No backdating, guessed relation or move. |
| 5. Backup guard `monitors mac-mini`, raw `protects` | PASS UNRESOLVED | Mini is an affected host, but the source is a backup-validation decision/guard. Full episode and decision companion establish refusing empty backup output, not monitoring the machine. Raw `protects` does not rescue canonical `monitors`. |
| 6. Blind cloud routine `runs_on hermes-ops` | PASS UNRESOLVED | The episode distinguishes retired cloud execution from the new launchd probe; its active-job companion identifies `comjhootchildscribe-probe`. Source metadata instead names an old Twilio routine, adding source-identity uncertainty. Retargeting only to Mini would preserve a wrong running source; repository is not a host. |
| 7. book-system `deployed_on mac-mini` | PASS KEEP_ENDPOINTS | Explicit rsync destination plus complete seed episode, shared data-plane and OrbStack companions support the machine endpoint. June 11 seed supports the installation context, not precision of the selected June 9 date. Preserve both, and do not endorse the contaminated repo ref or reversed AuthorClaw companion. |
| 8. build-with-codex skill `excludes hermes-ops`, raw `untested_end_to_end_on` | PASS UNRESOLVED | The sentence records a not-yet-run test awaiting Jeff's go-ahead, not exclusion of a repository. Full episode and wiring companions identify runtime/host context but cannot repair canonical semantics. No test execution authority is inferred. |
| 9. Build `contains hermes` | PASS UNRESOLVED | Full source episode concerns documentation of Hermes connection implementation in a repository. It does not establish containment of the standing gateway. The source entity is a backend service; repository/component versus service/runtime remains unresolved. |
| 10. Build `deployed_on hermes-ops` | PASS UNRESOLVED | README/mobile dogfood state is not deployment evidence. Full episode distinguishes desktop installation, fake dogfood agents, and a Mini control plane. Another `jbuild` record's Mini edge cannot settle this selected `build` service's source and destination. |

Exact per-key verdicts, all ten SHA-256 values and the unresolved missing-evidence descriptions are in `per-key-verdicts.json`; complete independently extracted facts, episodes and endpoint metadata are in `evidence-key-recheck.json` (initial run also retained as `evidence.json`). Reading companions does not approve their other ownership errors. These decisions neither legitimize the contaminated `hermes-ops` description nor endorse any existing aliases.

## Safety and scope

The six unresolved decisions are genuine abstentions, not silent keeps counted as repaired facts. In each, unsupported direction/relation or missing component identity prevents a settled ownership judgment. The four keeps retain their source, destination, full sentence, raw/canonical relation, dates, validity, confidence and provenance without edits. Broader `uses` for a test input in row 1 is compatible with the asserted role; it is unlike promoting `candidate_for` to established membership or `untested` to exclusion.

All writes were the independent archive, helper/evidence files and newly restored temporary stores. No provider, network research, live retry, live-store mutation, remember, shared-repository edit, deployment or source-project write occurred. No new fact-routing heuristic or repair manifest was created. No running-daemon or current health claims were tested; this gate does not regrade the five benchmark suites or certify the goal's unfinished recall floors.

