# Tenth ordered identity overlay — independent bounded disproof

2026-09-06. **GO for the exact private, uncalled, read-only planner, subject to the explicit scope limits below.** I failed to disprove its frozen finite contract through source inspection, all retained disproofs and characterizations, and fresh fixed-entry adversarial tests. This is an independent code verdict for the complete descriptive identity planner, not permission to activate it or a whole-goal grading round.

Reviewed frozen source: `/tmp/scry-ordered-overlay-tenth-sep06.NzgWqz/code`, baseline `e097fa6`, freeze SHA-256 `912c4592078c2697a5725b38c86c7e93956a73e9092d45b8fa2049918a132e93`. Input SHA-256 `a7e45e76b16b0aed90acaf9fb76cdad957a89bc321c80464f7566b9daade1242`; complete controlling contract SHA-256 `e56d63aa5b3461b8a4416f32697e9477f4afdd040d15e5c0e4e3cbcf099856ef`. Paths below are relative to the independent export `/tmp/scry-overlay-tenth-disproof.0l8awg` unless absolute.

## Fresh executable challenges

`TestTenthFreshInterleavedScopesAndWholePlan` establishes an actual synthetic Atlas generation using the unchanged generation helper, including its stored birth episode and canonical generation record. Three optional aliases exercise independently interleaved queries: Polar Manual has only a stored vote bearing the current input's episode ID; Lunar Handbook has two separate prior episode votes; Stellar Ledger has no matching rows. Repeated declarations exercise cached Polar Manual and Stellar Ledger decisions after intervening proposals. Their outcomes remain one rejected current-episode vote, a proposed independently attested alias, and rejected empty stored evidence respectively.

The fixture includes deliberately malformed bytes at exact prefix-adjacent addresses, old legacy attestation addresses and a foreign-generation scope. All remain opaque and unconsumed. The test independently enumerates the complete raw fixture, selects and sorts exact matching keys, and recomputes every returned prefix digest using its domain and length framing. It verifies Version, prefix, nonnil empty key list, count, sorted keys and raw digest for all three views. Every one of the five initial/cached consumers identifies its own scope and retains all exact positive rows and actual episode witnesses. No extra scope or excluded witness appears.

The same full original episode also combines an incompatible Cobalt daemon declaration, first-description/type preservation, a value-typed queue.go artifact, an independently useful assertion, a later rejected primary that discovers Nimbus Core, an earlier status hint that sees that discovery, an empty-relation slot with a present empty hint, and inverse same-slug mentions. Typed deferral does not become a status attribute; unrelated utility survives; source-side mapping remains original; the hint becomes related_to the later candidate. It checks all returned decision/declaration/primary/hint traces for valid producer references, decision-topological order, exact positive/negative raw witnesses, and resolvable query references. Mutating returned prefix keys/digests and every nonempty raw witness cannot change storage or a fresh byte-identical plan. The unchanged helper additionally verifies complete original slots/revision, every raw row, zero emitted events, empty actual identity/fact writer histories and no materialized candidates.

`TestTenthFreshRelevantGenerationRowsRefuse` supplies seven independent malformed relevant-evidence cases: present-empty attestation, extra key suffix, wrong body alias, wrong body generation, missing actual episode, duplicate known episode ID fields, and a non-time episode timestamp. Every case reaches the genuine fixed `planIdentityEpisode` through the unchanged error helper and returns its sanitized sentinel with a zero report, full raw equality and zero events. These are actual synthetic baseline rows, not forged owners, support reports or replacement finalizers.

All fresh tests passed on their first run. No compile failure, fixture correction, changed assertion or rejected fresh source exists in this review. The sole new test SHA-256 is `ea16c92f2de6ab48fd519f77a8d9481593694940611c84a0cadec47d22215882`.

## Complete finite-unit audit

Session orientation ran first and its output was read. I read the complete active objective, tenth through third review inputs, complete controlling contract with embedded independent corrections, all nine preceding complete disproof reports, delayed-birth design/review, ControllerV3 and identity-mutation reviews, registration contract, episode-selector contract, and the selector's linked complete design/review. Oversized combined output was followed by bounded reads of affected portions. A read-only rg expression initially had an unmatched group; it was corrected. A later no-match writer search returned rg's normal status 1. Neither diagnostic altered files or tests.

All eight overlay implementation files were read completely. I inspected the neutral DTO/projection, declaration and resolution adapters, original resolver declaration/primary/hint/alias paths and relevant lexical predicates; actual registry, serialized owner/coordinator and fixed finalizers; complete identity/reference inventories and identity/fact mutation and writer ledgers; active legacy recognition and generation/actual EP provenance. The exact source-declaration token verifier checks the full mechanical policy port; no new dictionary or routing threshold was introduced.

The retained test selection passes all supplied overlay groups, all nine prior independent suites, ordered-contract/delayed-birth characterizations and policy differential/adaptor tests. All 74 previously failing independent leaves therefore remain fixed. Coverage includes full original slots, nil/present Supersedes and nil/empty collections; inversion and value-source original-side mapping; first-discovery order and later links; rejected-discovery visibility to later primaries and earlier hints; typed failed-declaration persistence and independent trusted replacement; exact-natural/canonical-homonym/distinct-name explicit alias precedence; old metadata-only utility and all old repository references; missing/unlisted/stolen, consumed/retired/unadopted, current/history-only deferrals with unrelated utility; metadata/removal/released-claim producer chains; malformed exact raw alias suffixes and paired classification/rejection controls; derived natural identity endpoints and complete listing relationships; current-episode deduplication and legacy/generation isolation; actual stored EP evidence including current-input IDs; owned data/reopen; genuine owner/phase/poison and discarded-transaction read failure.

The ninth reference-evidence correction retains exact current/historical positive rows, explicit empty query scopes and snapshot digest/count binding through both missing-identity and fact-bearing-owner consumers. The retained independent reference matrix verifies self-loop counting, incoming references, historical exclusion for current-only queries, Value not being an endpoint, and report isolation. These views describe identity veto evidence, not factual support.

The tenth generation helper validates its exact `iga:<generation>:<alias-digest>:` boundary, checks genuine phase/owner/poison before any cache shortcut, and owns sorted keys/count/framed raw digest including an empty result. Positive rows still pass the separate canonical key/body and actual EP checks in vote. Initial and cached vote consumers merge PrefixQueries and exact point/EP dependencies; dispose clears both query caches. The new helpers' invalid-phase cache paths were inspected statically; the retained phase suite exercises the common guards. I do not claim a newly authored direct invalid-phase harness for the reference/prefix helpers.

Static inspection finds no raw or ordinary entity, fact, alias, vote or EP writer in the eight planner files. The entry owns original bytes before waiting, binds its own program to genuine serialized registration, registers before candidate proposals, and links later original observations. It accepts no selected owner, support flag or caller finalizer and returns no active facade/handle. Its freeze requires empty actual writer histories; the actual outer fixed finalizer verifies complete captured identity inventory and FA conservation. Independent successful/error fixtures preserve all raw rows and zero events.

The retained Badger characterization still establishes that stale zero-write Commit can return nil. No dummy write exists. This verdict covers the fixed program's absence of writers, same-snapshot/cooperative serialization and final captured-inventory conservation. It is not arbitrary raw-writer serialization, transient raw-write detection, protection from concurrent escaped facades, or production all-writer enforcement.

## Executed checks and pin discipline

Every Go command used `CGO_ENABLED=0` and `TMPDIR=/tmp/scry-overlay-tenth-disproof.0l8awg/test-tmp`. Complete test/check stdout and stderr were retained with tee under pipefail. Package durations are outcomes, not performance measurements.

| Command / log | Result |
| --- | --- |
| `ruby verify-pins.rb copy` → `initial-pins.log` | Both exports: 600 baseline SHA-256/git-blob pins, 57 addition SHA-256/byte-length pins verified around apply_patch copying; 15 external input/contract/report/verifier pins |
| Retained planner/nine-prior-disproof/characterization/differential selection → `retained-tests.log` | PASS store 10.947s / resolve 2.401s |
| `go test ./internal/memory/store -run TestTenthFresh -count=1 -v` → `fresh-tests.log` | PASS store .697s; mixed whole-plan scope test plus seven malformed-evidence leaves |
| Relevant store/identitypolicy/resolve `go vet` → `vet.log` | PASS, empty output |
| Exact pinned `verify_policy_port.go` and manifest → `policy-tokens.log` | PASS 157 declarations / 248 symbols; no extra copied declarations |
| `go test ./... -count=1` → `full-suite.log` | PASS all packages; store 73.902s / resolve 18.437s / daemon 28.650s |
| `ruby verify-pins.rb` → `final-pins.log` | PASS both complete exports: all 600 baseline SHA/blob and 57 addition SHA/byte pins; all 15 external pins unchanged |

The retained differential reports 2,323 literals, 16 unary predicates, 18,084 alias projections and 264 declaration projections. The exact neutral verification permits only the documented DTO/name substitutions. The full suite retains the unchanged original resolver corpus and 39-relation vocabulary tests. This is mechanical/adaptor parity and finite routing regression evidence, not a fresh held-out status/name semantic quality grade or permission to tune word lists. Production lexical deduplication remains mandatory later work.

The export was freshly created using `git archive e097fa6 | tar`. All 57 manifest additions were copied with apply_patch after full hash/byte validation. All authored tests/scripts/report/evidence-index files use apply_patch; gofmt touched only the new test. Both frozen and independent exports are verified at start and end. No supplied/baseline/frozen/shared file was edited. No actual stores, backups or replicas, SSH/providers, room/remember, configuration, deployment, cleanup outside synthetic fixtures or additional agents were used. Root's full-suite completion was inspected only as background. The separate complete-admission WIP and root actual-replica probes were neither copied nor graded. Complete test/script/log hashes are in `EVIDENCE_SHA256.txt`; the report hash is returned separately.

## Verdict limits

This failed-disproof GO applies only to the exact finite descriptive planner above. It does not approve FA identity/temporal/hint selection or virtual backdating; assertion-address/content/time/confidence/provenance preservation; actual support, materialization or B votes; final alias ownership closure; generation/legacy lifecycle adoption or all writers; Force/current-result integration; production lexical deduplication; replacing ControllerV3; prevention/deployment/live cleanup; actual recall, binaries/performance/sweeps; or any full-goal clause or either required full-goal grading round. Those remain mandatory later obligations. No new prerequisite outside this finite unit was imposed to reach this verdict.
