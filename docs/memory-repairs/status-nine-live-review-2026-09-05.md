# Independent actual-live nine-status retirement grade — 2026-09-05

**PASS for the bounded actual nine-status repair.** The actual pre-apply store is byte-identical at the complete logical-key/value level to the approved stable source. The actual post-apply store exactly matches the independently predicted nine-entry raw delta. All 80,242 facts, including 7,793 historical facts, retain their content and provenance. No unintended key/value change, fact loss, new graph defect, changed source owner, or new alias-routing defect was found.

The existing `dba-filing-guide` alias-routing defect is confirmed and remains unresolved. This result does not claim complete natural-source lookup success, broad cleanup completion, benchmark success, or full memory-goal completion.

The grader independently extracted deployed commit `393eeec79f80d3b4becff276c4fcffd71fa68ac5`, independently restored both actual backups, reconstructed the expected operation, reran failure/restore checks on disposable stores, and compared the entire actual post-store with the independently generated expected store. No builder result was used as proof. No live mutation, deployment, shared-repository edit, remember call, model call, or external research occurred. The exact CLI repeat was run against the restored actual post-store, not against live state.

## Exact artifacts

- Manifest: `/Users/jeff/workspace/context-stack/scry/docs/memory-repairs/status-nine-retirement-2026-09-05.json`, SHA-256 `a739d9d95de7ac8a8ff8e92ea33e3f717cb1cdf2696d09327c64dbc6bd94f36a`.
- Actual pre-apply backup: `/tmp/scry-migration0160-fresh-sep05.teYtyC/memory-20260905T202710Z.badger`, **77,910,559 bytes**, SHA-256 `9df09f52cdad787894c2996ae145d62b0f5d33e2c38c9962d7dec0fc3c30532c`.
- Actual post-apply backup: `/tmp/scry-migration0160-fresh-sep05.teYtyC/memory-20260905T202741Z.badger`, **77,914,543 bytes**, SHA-256 `b87def29a4334517299b55e9fc288de141f509b873c4afbdfd79bd101208ae06`.
- Approved stable source / actual pre / independent pre-backup restore raw logical hash: `4f892a3d5a3f0e214ebb66d7dd28051012259bf2a90370129bcfb469f02ebaf0`.
- Independently predicted / independently applied replica / actual post raw logical hash: `f5ecee171b64edec9d5d7f38bceab78b5c85fba0806b693506b3b0b142678927`.
- Fresh independent replica backup `independent-preapply.badger`: SHA-256 `9cf887a60702854df2370881d9aaceb751954e8c23cc4725e13f2bb9bfd231e0`; restored and matched against the complete pre-state.

Backup sizes and hashes were independently measured. Logical hashes use sorted JSON encodings of complete raw Badger keys and values; these are distinct from physical backup-file hashes.

## Source drift, semantic closure, and exact changes

`stable-to-actual-drift.json` proves **zero changed raw keys** between the stable source that passed the preceding gate and the actual pre-apply backup. Complete regenerated semantic evidence is also byte-identical, SHA-256 `898a118b79553eb459c3b0f29c7af8506f2bc21acf1f27e32db347d8178ce85e`. The nine candidates remain measurements/activity states with one current incoming fact each, no outgoing facts, and no historical touching facts. Their sources, full source episodes, companion facts, and artifact distinctions remain as reviewed.

Exactly these candidate nodes were retired: `1-complete-priority-cell`, `164-fields-have-field-evidence`, `193-pages-blocked-on-authority-verification`, `21-suspect-bodies-removed`, `50-jurisdictions-published`, `641-fields-have-content`, `actively-building-maine`, `addressing-review-findings`, and `ai-sync-tests-15-passing`.

The complete actual raw delta consists of **36 deleted records and 27 added records**:

- Delete nine candidate entity records, nine reviewed alias-index claims, nine original edge facts, and nine canonical reverse indexes.
- Add nine attribute facts containing the exact retired display-name literal, nine durable slug tombstones, and nine durable spelling tombstones.

No unrelated raw record changes. Every original fact is compared as complete JSON, preserving source, relation, raw relation, text, validity, timestamps, confidence, provenance, and unmodeled fields; only each selected `Dst` becomes empty and its `Value` becomes the exact reviewed display name. All actual post-store records match the independently constructed expected raw map, including complete tombstone content. No source is guessed or reassigned.

All alias-index claims and outside entity listings were reviewed on the identical pre-state; candidate claims are exclusive, with no extra stale route or outside normalized/hygiene-fold listing. All noncandidate alias records remain byte-for-byte unchanged in actual post. No retired endpoint or stale candidate adjacency remains. `43-url-sitemap` and all other noncandidate artifacts remain unchanged.

The three factual caveats remain unresolved: the stored 193-blocked-pages and 641-content-fields assertions conflict with their episode's 1,062/193 counts, while fifty seeded jurisdictions are not independent proof of fifty published jurisdictions. These assertions are preserved verbatim as values; the repair neither corrects nor endorses them.

## Measured actual result

| Measurement | Actual pre | Actual post |
|---|---:|---:|
| Entities | 30,457 | 30,448 |
| Facts | 80,242 | 80,242 |
| Historical facts | 7,793 | 7,793 |
| Episodes | 9,325 | 9,325 |
| Current relations | 39 | 39 |
| Cross-type collisions | 482 | 482 |
| Self-loop facts | 1,095 | 1,095 |
| Dangling-endpoint facts | 1,995 | 1,995 |
| Hollow entities | 2,855 | 2,855 |
| Logical raw keys | 241,498 | 241,489 |

The complete relation set and complete self-loop/dangling/hollow lists are identical before/after. Existing global defects remain broader-goal failures. All pending, attempt, cursor, value-evidence, and metadata records are unchanged by actual apply. `PendingCounts` on both actual backups returns **0 ready / 0 backoff / 5 parked** at `2026-09-05T20:27:42Z`.

## Lookup result and explicit DBA caveat

All nine retired entity/name lookups are absent. All nine replacement values are present under direct lookup of their unchanged source slug. Source routing before and after is identical; the actual operation did not steal or rehome a source alias.

The daemon's deployed `resolveMemorySlug` consults the alias index before falling back to slugification, and `memory.facts` calls that resolver before reading facts. Independent restored-store checks reproduce:

| Input spelling | Alias owner before and after | Original edge visible before | Preserved value visible after |
|---|---|---|---|
| `dba-filing-guide` | `dbafilingguide` | No | No |
| `DBA filing guide project` | `dba-filing-guide` | Yes | Yes |

The exact preserved fact has source `dba-filing-guide` and now value `actively-building-maine`. The natural slug was already routed to the other project before retirement, so that lookup hid the original edge too. The alternate existing alias retrieves the correct source and preserved assertion. The repair neither caused nor fixed this defect. `dba-routing-caveat.json` and `source-routing.json` contain the measured routes. `ai-syncpy` lacks a direct alias claim but correctly reaches its existing slug through the deployed fallback; this is distinct from the DBA alias override.

No blind alias correction was performed, and this report does not certify universal canonical-source lookup correctness.

## Rollback, backup, and exact second preview

The actual pre-backup is nonempty and independently restorable; its complete logical state matches the approved source. Reconstructing the operation on that backup also passes the ninth-group drift rejection, all-nine postcondition rejection, nonempty independent backup restore, and exact postcondition checks. Both forced failures preserve the complete pre-state and emit zero events. These are independent replica failure proofs, not claims that live rollback was attempted.

Against the actual post restoration, an independently executed exact deployed CLI command returns `dry_run=true`, `applied=0`, `refused=9`; all nine problems state that the corresponding retired entity does not exist:

```text
go run ./cmd/scry memory retire-entities --file /Users/jeff/workspace/context-stack/scry/docs/memory-repairs/status-nine-retirement-2026-09-05.json --dir /tmp/scry-status-nine-live-grade.kSZm7u/actual-post-restored
```

No `--apply` was provided. A subsequent independent complete raw-state comparison proves the exact CLI preview changed nothing. Its full output is retained in `exact-cli-second-preview.json`; the no-write result is `cli-preview-no-write.json`.

## Evidence and scope

Key artifacts in this directory: `actual-live-results.json`, `stable-to-actual-drift.json`, `dba-routing-caveat.json`, `source-routing.json`, actual post entity/fact exports, independently generated pre/post entity/fact exports, `semantic-evidence.json`, `independent-previews.json`, `mechanical-results.json`, `graph-audit.json`, `raw-delta.json`, `raw-payload-and-lookup.json`, the exact CLI output, the no-write check, independently restored actual stores, and grader-written tests.

Independent tests passing: `TestStatusNineIndependent`, `TestStatusNineActualLive`, `TestStatusNineIndependentRawPayloadAndLookup`, `TestStatusNineIndependentGraph`, and `TestStatusNineExactCLIPreviewNoWrite`. No benchmark, subsequent real sweep, or future code deployment is graded here. The five recall suites being collected by the lead remain separate evidence, and this report must not be used to declare the overall goal complete.
