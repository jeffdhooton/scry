# Jev diagnostic hardening and bounded shadow trial

User authorization: improve error reporting, harden the integration, and start a small shadow trial. Existing Jev integration and frozen evaluations remain intact. Branch remains `feat/jev-memory-integration` with prior user work preserved.

1. Add safe typed transport/response failure categories in `assess`; propagate categories into worker results and status aggregates. Keep strict validation, no retries, pinned model, bounded response, and no credential/body logging. Test actual local HTTP failure boundaries.
2. Add durable dispatch reservation count and per-job reservation in `assessstore`. A cap must count uncertain attempts before network dispatch, survive reopen/cleanup, and serialize concurrent reservations. Add optional validated trial configuration with source cutoff, absolute expiry and maximum requests. Worker refuses dispatch at cap/expiry, and resume cannot bypass either.
3. Wire trial eligibility and status into daemon: only raw episodes occurring at/after the trial start, no old graph-summary import, stop capture after expiry/cap. Preserve primary extraction behavior. Add behavioral tests for invalid configuration, eligibility, limit concurrency/restart, typed error propagation and health visibility.
4. Review changes and run full relevant tests, races and vet. Build a uniquely versioned binary. Take a verified nonempty graph backup; retain old binaries/config. Deploy identical binary to laptop and Mini, supply Mini daemon credential without echo or YAML secrets, enable a 100-request/24-hour trial with concurrency 1. Restart gracefully and verify memory/extraction/assessment health and one inspectable trial outcome. Do not replay old failures or ingest old historical data.

Design ruling: the cap is lifetime dispatch reservations in this sidecar; zero is unlimited outside a trial. Reservations are never decremented or reset by retention. A future trial requires an explicit larger cap, avoiding restart/reset loopholes.

Completed: safe failure codes, durable cap/window, source cutoff and status metrics implemented, regression tested and independently reviewed. Full suite, focused races and vet passed. Byte-identical binary deployed to laptop and Mini after verified 144,403,617-byte graph backup, with prior binaries/config retained. Trial active 2026-09-20T13:12:56Z through 2026-09-21T13:12:56Z, 100 attempts/concurrency1. First operational note produced 15/15 completed assessments, zero errors; primary extraction remained healthy. Evidence and rollback: `docs/memory-assess/trial-2026-09-20/README.md`.
